# Ouvoca 使用者操作手冊（繁體中文）

> **適用版本**：v3.23 對話式 ERP（Ouvoca）
> **目標讀者**：第一次用 ERP 的老闆、業務、廠長、採購、倉管
> **預計閱讀時間**：30 分鐘上手，遇到狀況再翻 §11 疑難排解
> **語言**：本系統支援繁體中文 🇹🇼 與 English 🇺🇸 即時切換

> 🆕 **v3.13-v3.16 新功能（必看）**
> - ⚙️ **設定頁**：自己載示範資料、清示範資料、上傳業務文件（報價單/發票/合約）、設定 AI API Key（§3.5）
> - 🤝 **CRM 頁**：Lead 漏斗、商機 Kanban、客戶 360 視圖、活動 timeline（§3.6）
> - 💡 **AskAI 浮球**：右下角永遠在的「現場 AI 教練」，問「這頁怎麼用」AI 直接教（§3.7）
> - 🤖 **AI 自動記 activity**：你下訂單、轉客戶、推商機階段時，CRM timeline 自動有記錄（不必手動加）

> 🚀 **v3.17-v3.22 新功能（最近 6 sprint，2026-05-18）**
> - 📝 **每頁可建單**：Sales/Purchase/Production 加新增主實體 + 快速建單 1 行 UI（§3.8）
> - 📒 **會計 + 電子發票**：傳票 / AR / 科目表 / 電子發票開立查詢作廢（§3.9）
> - 📈 **報表中心**：DSO/庫存週轉/毛利率 即時 + AR aging xlsx + 401 報表（§3.10）
> - 🌍 **多國統編驗證**：6 國內建（TW/CN/US/JP/EU+GENERIC）+ 可自定（§3.11）
> - 🔍 **Cmd+K 全系統搜尋**：按 Ctrl+K / Cmd+K 模糊搜客戶/料件/訂單（§3.12）
> - 🖨 **單據列印 PDF**：PO/SO/出貨單/發票 一鍵印給供應商/客戶（§3.13）
> - ✅ **多階審批工作流**：規則設定 + 待我審 + 歷史（§3.14）
> - 📊 **流程鏈視覺化**：點 📊 看單據在流程哪一步（§3.15）
> - 📝 **單據備註**：每張 PO/SO/WO 可留 internal notes（§3.16）

> 🆕 **v3.23 新功能（2026-05-18 Sprint Q）**
> - 📋 **Dashboard 待辦中心**：登入第一眼看「待我審 / 缺貨警示 / 草稿 PO / 草稿 WO」（§3.17）
> - 📖 **產品做法 (Recipe) 編輯器**：原 BOM 改名「做法」（不抄鼎新詞）— 視覺化 + **unblock WO release**（§3.18）
> - 📜 **庫存異動歷史**：Inventory 頁加 tab，看每筆 inbound/outbound（§3.19）

> 🎨 **v3.24 Ouvoca 原創語彙（不抄對手，給小白好記）**
> - 🌱 **新苗 (Sprout)** = 原 Lead 潛在客戶（種子發芽變客戶）
> - 🎯 **追單 (Chase)** = 原 Opportunity 商機（業務每天追的單）
> - 📖 **做法 (Recipe)** = 原 BOM 物料表（像食譜，產品由什麼料件組成）
> - 👤 **客戶全貌** = Customer 360 視圖
>
> 雙語對照（i18n）：見 `src/i18n/locales/{zh-TW,en}.ts` 的 `ouvocaTerms`

> 🏛️ **v3.25 新功能：「家規 (House Rules)」** — Ouvoca 招牌差異化
> - **不抄 SAP/鼎新/Odoo 寫死 rule**，Ouvoca 規則資料化：UI 開關 / AI 對話建 / Plugin 擴充
> - 預設 3 條家規：WO 釋放需做法、PO > 10 萬需主管審、PO 必須至少 1 項目
> - 對話建規矩：「我們公司 SO 折扣超 5% 應該主管審」 → ConfirmCard 確認 → 立即生效
> - 主管覆寫 + 完整 audit log
> - **完整使用指南**：[`docs/HOUSE_RULES_GUIDE_ZH.md`](./HOUSE_RULES_GUIDE_ZH.md) 🇹🇼 / [`HOUSE_RULES_GUIDE_EN.md`](./HOUSE_RULES_GUIDE_EN.md) 🇺🇸

---

## 給第一次讀這份手冊的你

如果你是電腦小白，看到「ERP」三個字就頭痛——**請放心，我們重新設計了**。

這套 Ouvoca 不需要你學選單在哪裡、欄位叫什麼、按鈕順序是什麼。
**你只要會打字，就會用**。打開電腦，在對話框輸入：

> 「列出今天進行中的工單」
> 「跟長江廠下 100 個 M6 螺絲，交期下週五」
> 「把 SO-2025-0042 的交期改到 6/10」

AI 助手會理解你的意思，幫你查、幫你下單、幫你改單。
**所有會動到資料的動作（下單、改單、刪單）系統都會跳出「確認卡」要你按確認**——按錯了還有 90 秒可以撤銷。

---

## 目錄

1. [系統簡介（5 分鐘）](#1-系統簡介)
2. [第一次登入（3 分鐘）](#2-第一次登入)
3. [介面導覽（5 分鐘）](#3-介面導覽)
   <details>
   <summary><b>　└ v3.60–v3.69 新功能操作教學（15 節，點開看）</b></summary>

   | 節 | 功能 | 版本 |
   |---|---|---|
   | 3.20 | [請購單 PR → 採購單 PO](#320--請購單-pr--採購單-po--v363-新增) | v3.63 |
   | 3.21 | [收料：整批收 / 只收一部分 / 收料單 GRN](#321--收料整批收--只收一部分--收料單-grn--v363-新增) | v3.63 |
   | 3.22 | [RFQ 詢價 → 比價 → 決標轉採購單](#322--rfq-詢價--比價--決標轉採購單--v364-新增) | v3.64 |
   | 3.23 | [領料單 MI：工單從倉庫領料](#323--領料單-mi工單從倉庫領料--v363-新增) | v3.63 |
   | 3.24 | [退貨單 RT：客戶退貨回來](#324--退貨單-rt客戶退貨回來--v363-新增) | v3.63 |
   | 3.25 | [條碼槍掃描 + 料件 QR 標籤列印](#325--條碼槍掃描---料件-qr-標籤列印--v364-新增) | v3.64 |
   | 3.26 | [批號與序號正反向追溯](#326--批號與序號正反向追溯--v364-新增) | v3.64 |
   | 3.27 | [三大報表：試算表 / 損益表 / 資產負債表](#327--三大報表試算表--損益表--資產負債表--v361-新增) | v3.61 |
   | 3.28 | [台灣 401 / 405 營業稅申報表](#328--台灣-401--405-營業稅申報表--v361-新增) | v3.61 |
   | 3.29 | [供應商發票 3-Way Match（三方勾稽）](#329--供應商發票-3-way-match三方勾稽-v361-新增) | v3.61 |
   | 3.30 | [固定資產與每月折舊](#330--固定資產與每月折舊--v361-新增) | v3.61 |
   | 3.31 | [銷貨成本月結（COGS 結轉）](#331--銷貨成本月結cogs-結轉-v361-新增) | v3.61 |
   | 3.32 | [雙重驗證 MFA（手機驗證碼）](#332--雙重驗證-mfa手機驗證碼-v364-新增) | v3.64 |
   | 3.33 | [備份與還原](#333--備份與還原--v362-新增) | v3.62 |
   | 3.34 | [系統組態 14 項](#334--系統組態-14-項--v360-新增) | v3.60 |

   </details>
4. [對 AI 講話：4 種 CRUD 操作（10 分鐘）— 重點章節](#4-對-ai-講話4-種-crud-操作)
5. [ConfirmCard 確認卡（3 分鐘）— 重點](#5-confirmcard-確認卡)
6. [Slot-filling 反問機制（3 分鐘）](#6-slot-filling-反問機制)
7. [90 秒 Undo 撤銷（2 分鐘）](#7-90-秒-undo-撤銷)
8. [4 個角色實戰（10 分鐘）](#8-4-個角色實戰)
9. [三軌授權（2 分鐘）](#9-三軌授權)
10. [常見問題（5 分鐘）](#10-常見問題)
11. [疑難排解（5 分鐘）](#11-疑難排解)

---

## 1. 系統簡介

### 1.1 Ouvoca 是什麼？

Ouvoca 是一套**為 50-100 人小型製造業設計**的「對話式 ERP（企業資源規劃）」系統。

「ERP」翻成白話就是：**幫公司管庫存、管訂單、管採購、管生產、管報表**的軟體。
傳統 ERP（像 SAP、Oracle）要花幾百萬買、員工受訓 1-3 個月才會用。

**Ouvoca 把這件事翻轉**：
你不用學介面，你用「講話」（打字）就能操作整套系統。

### 1.2 三大核心承諾

| 承諾 | 白話翻譯 |
|---|---|
| **🗣️ 自然語言操作** | 你想做什麼，就打字告訴 AI，它會幫你做。不用學選單在哪裡。 |
| **🛡️ 全程確認 + 90 秒撤銷** | 任何會動到資料的操作（下單/改單/刪單），AI 都會跳「確認卡」讓你按。按錯了 90 秒內可以撤銷。 |
| **⚡ 即時、不等月底** | 老闆隨時問「今天狀況」，10 秒內拿到答案，不用等月報。 |

### 1.3 適合誰用？

這套系統設計給**「50-100 人小型製造業」**的四種角色：

| 角色 | 主要工作 | 用什麼裝置 |
|---|---|---|
| 👔 **老闆（王董）** | 隨時掌握公司狀況、看財務數字 | 辦公室桌機 Chrome 瀏覽器 |
| 👨‍💼 **業務（小陳）** | 在客戶面前秒答庫存、報價、交期 | 筆電（外出帶 Chrome 連 VPN） |
| 👨‍🏭 **廠長（林廠長）** | 看哪裡卡關、釋放工單、調整排程 | 辦公室桌機 + 廠房大屏 |
| 👩‍💻 **採購兼倉管（阿玲）** | 下採購單、收料、盤點 | 桌機 + USB 條碼槍 |

### 1.4 你能用它做什麼？

四類操作（業界術語叫「CRUD」）：

| 簡寫 | 中文 | 你會講的話舉例 |
|---|---|---|
| **C** = Create | 增 | 「跟長江廠下 100 個 M6 螺絲」 |
| **R** = Read | 查 | 「列出庫存低於安全庫存的零件」 |
| **U** = Update | 改 | 「把 SO-2025-0042 的交期改到 6/10」 |
| **D** = Delete | 刪 / 取消 | 「取消 PO-2025-0107」 |

---

## 2. 第一次登入

### 2.1 開啟系統

請 IT 把系統部署好之後（如果還沒，請看 `INSTALLATION_ZH.md`），你只要做這一步：

1. 打開 **Chrome 瀏覽器**（推薦最新版；Edge / Firefox 也可以）
2. 在網址列輸入：`http://localhost:5173`（IT 給你的網址）
3. 按 Enter

你會看到登入畫面：

```
┌─────────────────────────────────────────┐
│                                  🇹🇼 🇺🇸 │
│                                          │
│            Ouvoca 對話式 ERP            │
│                                          │
│         ┌──────────────────────┐         │
│         │ 帳號                  │         │
│         │ ┌──────────────────┐ │         │
│         │ │ admin             │ │         │
│         │ └──────────────────┘ │         │
│         │                       │         │
│         │ 密碼                  │         │
│         │ ┌──────────────────┐ │         │
│         │ │ ••••••••          │ │         │
│         │ └──────────────────┘ │         │
│         │                       │         │
│         │   [ 登入 ]            │         │
│         │                       │         │
│         │  ── 或 ──             │         │
│         │  [ 以 Demo 模式進入 ] │         │
│         └──────────────────────┘         │
│                                          │
└─────────────────────────────────────────┘
```

### 2.2 輸入帳號密碼

| 步驟 | 操作 |
|---|---|
| 1 | 滑鼠**點「帳號」欄位** |
| 2 | 鍵盤打字輸入帳號（首次預設：`admin`） |
| 3 | 用 Tab 鍵或滑鼠**點到「密碼」欄位** |
| 4 | 鍵盤輸入密碼（首次預設：`admin123`） |
| 5 | 滑鼠**點藍色「登入」按鈕**（或按 Enter） |

> ⚠️ **第一次登入後請立刻改密碼**！
> 進入後：右上角頭像 → 個人設定 → 修改密碼

### 2.3 不想記帳號？用 Demo 模式

如果你只是想試試看，不想記帳號：
- 滑鼠**點下方「以 Demo 模式進入」按鈕**
- 就會直接進入系統，所有功能都能玩

> ⚠️ Demo 模式上線後會被 IT 關掉。正式上線一定要用真實帳號。

### 2.4 切換語言

登入畫面**右上角**有兩個國旗按鈕：

| 圖示 | 意思 |
|---|---|
| 🇹🇼 | 切換到繁體中文 |
| 🇺🇸 | 切換到 English |

登入後一樣在**右上角**可以切換。**切換是即時的**，不用重新登入。

---

## 3. 介面導覽

### 3.1 主畫面長什麼樣？

登入成功後，你會看到這樣的畫面：

```
┌──────────────────────────────────────────────────────────────────┐
│  [左側 Sidebar]      │  [右上 Header]                              │
│                       │           🇹🇼 / 🇺🇸    👤 admin   [登出]   │
│  🏠 首頁              │                                              │
│  📊 儀表板            │                                              │
│  ─────────           │    主內容區（依當前頁面變化）                │
│  📦 庫存              │                                              │
│  🛒 採購              │    例如點「儀表板」就會顯示：                │
│  💰 銷售              │       - AI 智能摘要                          │
│  🏭 生產              │       - 4 張關鍵卡片                         │
│  🔬 品質              │       - 最近工單                             │
│  ─────────           │       - 庫存警示                             │
│  📈 報表              │                                              │
│  ⚙️  設定             │                                              │
│                       │                                              │
│  ─────────           │                                              │
│  💬 AI 助手 (Chat)    │  ← 點這裡開「對話框」就可以打字問 AI         │
└──────────────────────────────────────────────────────────────────┘
```

### 3.2 左側 Sidebar 17 大頁面（v3.22 起）

分 3 區塊：**總覽** / **營運** / **系統**

#### 📊 總覽區
| 順序 | 圖示 | 名稱 | 用途 |
|---|---|---|---|
| 1 | 📊 | **儀表板** | KPI 卡片（營收/工單/庫存警示）|
| 2 | 💬 | **AI 助手** | 對話式 CRUD：用講的查/增/改/刪 |
| 3 | 📡 | **事件流** | 即時看系統發生什麼事 |

#### 🏭 營運區
| 順序 | 圖示 | 名稱 | 用途 |
|---|---|---|---|
| 4 | 📦 | **庫存** | 料件清單、庫存查詢、安全庫存警示 |
| 5 | 🛒 | **採購** | PO 建立 / 核准 / 進貨 / 取消 + 流程鏈 + 列印 + 備註 |
| 6 | 🏭 | **生產** | WO 建立 / 釋放 / 完工 / 取消 + 流程鏈 + 備註 |
| 7 | 💰 | **銷售** | SO + 客戶 + 確認 / 出貨 + 列印（含出貨單） + 流程鏈 + 備註 |
| 8 | 🤝 | **CRM** | Lead 漏斗 / 商機 Kanban / 客戶 360（§3.6）|
| 9 | 📒 | **會計** ✨ | **v3.18** 傳票 / 應收帳款 / 科目表（§3.9）|
| 10 | 🧾 | **電子發票** ✨ | **v3.19** 開立 / 查詢 / 作廢（§3.9）|
| 11 | 🔬 | **品質** | 檢驗單（唯讀稽核用，依設計不能改）|
| 12 | 📈 | **報表** ✨ | **v3.19** KPI / AR aging / 401（§3.10）|
| 13 | ✅ | **審批** ✨ | **v3.22** 多階審批：規則 / 待我審 / 歷史（§3.14）|

#### 🛡 系統區
| 順序 | 圖示 | 名稱 | 用途 |
|---|---|---|---|
| 14 | 🛡️ | **權限管理** | 角色、權限分派（admin only）|
| 15 | 🔑 | **我的權限** | 看自己有哪些權限 |
| 16 | ⚙️ | **設定** | AI 設定 / 示範資料 / 上傳檔案（§3.5）|

💡 **17 頁太多找不到？** 按 **Ctrl+K**（Mac: Cmd+K）開啟全系統搜尋（§3.12）。

### 3.3 右上 Header 三個重點

| 元素 | 用途 |
|---|---|
| 🇹🇼 / 🇺🇸 | 即時切換中英文 |
| 👤 admin（你的名字） | 點開可以改密碼、看個人權限 |
| **[登出]** | 結束本次工作，建議下班前點一下 |

### 3.4 💬 AI 助手 (Chat) 在哪？

這是 **v3.x 最重要的功能**。在左側 Sidebar 點 **「💬 AI 助手」**，會跳出一個對話框：

```
┌──────────────────────────────────────────────────────┐
│  💬 Ouvoca AI 助手                              ✕  │
├──────────────────────────────────────────────────────┤
│                                                       │
│  AI：您好！我是 Ouvoca 助手。您可以對我說：           │
│       「列出今天進行中的工單」                          │
│       「跟長江廠下 100 個 M6 螺絲」                     │
│       「把 SO-2025-0042 的交期改到 6/10」              │
│                                                       │
│  ─────────────── 你的對話歷史會顯示在這 ────────────  │
│                                                       │
│                                                       │
├──────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────┐  │
│  │ 請輸入你的問題...                              │  │
│  └──────────────────────────────────────────────┘  │
│                                          [ 送出 ]   │
└──────────────────────────────────────────────────────┘
```

**怎麼用？**

| 步驟 | 操作 |
|---|---|
| 1 | 滑鼠點「請輸入你的問題」框 |
| 2 | 鍵盤打字，例如：「列出今天進行中的工單」 |
| 3 | 按 Enter（或點「送出」按鈕） |
| 4 | 等 2-10 秒，AI 會回答 |

### 3.5 ⚙️ 設定頁 (Settings) ✨ v3.13 新增

點左側 sidebar 的 **「⚙️ 設定」** 進到設定頁，有 4 個區塊（**所有電腦小白會用到的東西都在這頁**）：

#### A. 🤖 AI 助手設定

第一次裝完 Ouvoca 還沒申請 LLM API Key？這裡讓你**自己貼進去**，不必碰技術指令。

| 步驟 | 操作 |
|---|---|
| 1 | 點 **「Provider」** 下拉 → 選「DeepSeek（推薦）」 |
| 2 | 點 **「還沒帳號？去申請 →」** 連結，按指示 5 分鐘拿 `sk-...` 字串 |
| 3 | 把 `sk-...` 貼到 **「API Key」** 欄 |
| 4 | 點 **「🧪 測試連線（不儲存）」** 看是不是 ✅ 綠燈 |
| 5 | 點 **「💾 儲存（即時生效）」** 完成（不用重啟）|

💡 詳細申請教學：[`HOW_TO_GET_LLM_API_KEY_ZH.md`](./HOW_TO_GET_LLM_API_KEY_ZH.md)

#### B. 📦 示範資料

系統內建 5 客戶 / 3 供應商 / 10 料件示範資料（DEMO- 前綴），試完可一鍵清除：

- **「➕ 載入示範資料」** → 把示範資料寫進 DB（可重複按，已存在會跳過）
- **「🗑 清除示範資料」** → 只清 DEMO- 前綴的，你自己加的不會被刪

#### C. 📁 上傳業務文件

把客戶的報價單 / 發票 / 規格書 / 合約等檔案傳進系統存著（將來 AI 可解析）：

| 步驟 | 操作 |
|---|---|
| 1 | 選分類（報價單 / 發票 / 合約 / 規格書 / 一般）|
| 2 | 寫一句描述（選填，例：「示範客戶 A 5/15 報價」）|
| 3 | **拖檔案到框框裡**，或點框框選檔 |
| 4 | 看到 ✅ 上傳成功就好 |

支援：PDF / Excel / CSV / Word / 圖片，每檔 25 MB 內。

#### D. ℹ️ 系統資訊

看版本、授權軌、商業諮詢連結。

### 3.6 🤝 CRM 頁 ✨ v3.15 新增

點左側 **「🤝 CRM」** 進客戶關係管理頁，3 個 tab：

#### Tab 1: 📋 Lead 漏斗

**Lead 是什麼？** 還沒成交的潛在客戶（從展會 / 廣告 / 介紹來的）。

4 欄漏斗：🆕 新進 → 📞 已接觸 → ✅ 已驗證 → ❌ 失敗（或 🎯 轉客戶）

```
┌──────────┬──────────┬──────────┬──────────┐
│ 🆕 新進   │ 📞 已接觸 │ ✅ 已驗證 │ ❌ 失敗   │
│  (3)     │  (5)     │  (2)     │  (1)     │
├──────────┼──────────┼──────────┼──────────┤
│ 公司 A   │ 公司 B   │ 公司 C   │ 公司 D   │
│ 王經理   │ 李經理   │ 張經理   │ 周經理   │
│          │          │ [🎯 轉客戶] │          │
└──────────┴──────────┴──────────┴──────────┘
```

點右上 **「➕ 新增 Lead」** 填公司名稱即可建檔。已驗證 (qualified) 的 Lead 可以一鍵 **「🎯 轉為正式客戶」**。

#### Tab 2: 💼 商機 Kanban

**商機是什麼？** 有付費意向的客戶，正在推銷中的案子。

5 欄階段：🔍 探索 → 📝 提案 → 🤝 議價 → 🎉 成交 → ❌ 失敗

每張卡片有 **「進到下一階段 →」** 按鈕，業務點一下就推進，**不必滑鼠拖**。
最上面顯示「加權總值 NT$ X」= 所有商機金額 × 勝率% 加總。

#### Tab 3: 👤 客戶 360

最強功能。**選一個客戶，看到全部相關資料**：

```
┌─客戶清單─┬─客戶 A 資訊───────────────┐
│ ▸ A     │ 📋 CUST-001 ⭐ A 級         │
│ ▸ B     │ 👤 王經理 📞 02-1234        │
│ ▸ C     │ ────────────────────────── │
│ ...    │ 訂單 5 / 商機 2 / 活動 18    │
│        │ ────────────────────────── │
│        │ 📋 訂單列表 │ 💼 商機列表  │
│        │ ────────────────────────── │
│        │ ⏱ 活動時間軸（自動 + 手動）  │
│        │  📋 銷售單 SO-0042 成立      │
│        │  🎯 從 Lead 轉為正式客戶     │
│        │  📞 電話拜訪：詢問報價       │
└────────┴───────────────────────────┘
```

✨ **Ouvoca 原創**：下訂單、轉客戶、推商機階段時，活動時間軸**自動有記錄**。
你只要在「特殊事件」時手動加（電話拜訪、客戶抱怨等）點右上「📝 加活動記錄」。

### 3.7 💡 AskAI 浮球 ✨ v3.16 新增（Ouvoca 獨家）

**右下角的紫色 💡 浮球**永遠在（除了 AI 助手頁本身）。

**用途**：每頁的「現場 AI 教練」。卡住的時候不用翻文件、不用看 FAQ，直接點開問。

```
                            ┌─────────────────┐
                            │ 💡 問 AI - 現場教練│
                            │ 針對你現在這頁回答 │
                            ├─────────────────┤
                            │ 🎓 我會在這頁陪你 │
                            │  問我：           │
                            │  「這頁怎麼用?」 │
                            │  「我卡住了」    │
                            │  「常用功能?」   │
                            ├─────────────────┤
                            │ [問我「...」]  ➤ │
                            └─────────────────┘
                                     │
                                     ▼
                                 💡  (脈衝)
```

**特色**：
- 它知道你**現在在哪個頁面**（會用上下文回答）
- 三個快速建議按鈕：點一下就送出常見問題
- 沒設 LLM API Key？它會跳出 3 步驟引導你去申請（不會冷冰冰報錯）

### 3.8 📝 每頁的「新增」按鈕 ✨ v3.17 新增

之前小白裝完 Ouvoca 在 Sales/Purchase/Production 點不到「新增」（只能用 AI 對話）。
v3.17 起每頁加 **QuickCreateBar**（頁面上方）：

| 頁面 | 新增按鈕 |
|---|---|
| Sales | `➕ 新增客戶` / `📝 快速建單(1項)` / `💬 用 AI 建多項目` |
| Purchase | `➕ 新增供應商` / `🛒 快速建採購單(1項)` / `💬 用 AI 建多項目` |
| Production | `➕ 新增產品` / `🏭 新增工單` / `💬 用 AI 釋放工單` |

點按鈕 → 行內展開 form → 填欄位 → 儲存。
**多項目訂單請用 AI 對話**（複雜情境）。

### 3.9 📒 會計 + 🧾 電子發票 ✨ v3.18-19 新增

#### 📒 會計頁 3 tab
- **📊 傳票**：列表 + 簡化版「1 借 1 貸」建傳票 + 草稿→過帳
- **💵 應收帳款**：列表 + 帳齡 + 狀態過濾（未收 / 部分收 / 已收清 / 逾期）+ 逾期紅色高亮
- **📚 科目表**：依類型（資產/負債/權益/收入/費用）分組顯示

#### 🧾 電子發票頁 3 區塊（台灣財政部 MIG 標準）
- **📝 開立發票**：填賣方統編/公司名 + 買方統編（**可選國別** v3.20）+ 多項目（品名/數量/單價）
  - 系統自動算未稅 + 5% 稅 + 含稅總計
  - 開立成功 → 跳出 ✅ + 「🖨 列印發票」按鈕
- **🔍 查詢發票**：用發票號查 MIG 完整資料
- **🚫 作廢發票**：必填原因（依稅法稽核要求）

### 3.10 📈 報表中心 ✨ v3.19 新增

4 個區塊：
1. **📊 KPI 即時儀表板**：DSO 應收天數 / 庫存週轉 / 毛利率（即時計算 + 解讀）
2. **💵 AR aging xlsx**：下載 Excel（全部 / 只逾期）
3. **📦 月度庫存報表 xlsx**：選月份 → 下載
4. **🧾 台灣 401 報表 HTML**：選年/期/公司名 → 開 HTML → 按 Ctrl+P 印 / 存 PDF 給國稅局

### 3.11 🌍 多國統編驗證 ✨ v3.20 新增

電子發票買方統編欄旁邊有「國別」下拉：

| 國家 | 格式 |
|---|---|
| 🇹🇼 台灣 | 8 碼 + checksum |
| 🇨🇳 中國 | 18 碼 GB 32100 |
| 🇺🇸 美國 | EIN 9 碼 |
| 🇯🇵 日本 | 法人番號 13 碼 |
| 🇪🇺 EU VAT | 國家碼+數字（DE/FR/IT 等 28 國）|
| 🌐 通用 | 不驗證（給未支援國家）|

**自己有特殊國家？** IT 可以用 `register_validator()` plug-in 自定（見 `docs/HOW_TO_GET_LLM_API_KEY_ZH.md`）。

### 3.12 🔍 Cmd+K 全系統搜尋 ✨ v3.21 新增（Ouvoca 招牌）

對標 SAP B1 / Linear / Notion / Raycast。

| 平台 | 按鍵 |
|---|---|
| Mac | **Cmd+K** |
| Windows / Linux | **Ctrl+K** |

打開後可搜：客戶 / 供應商 / 料件 / 產品 / SO / PO / WO / Lead，或 10 個快速命令（去 CRM / 設定 / 報表 / AI...）。
↑↓ Enter 鍵盤導航 / ESC 關閉。**任何頁都能用**。

### 3.13 🖨 單據列印 PDF ✨ v3.21 新增

每張單據 row 加 🖨 按鈕（鼎新 / 正航 / SAP 標準功能）：

| 頁面 | 列印什麼 | 用途 |
|---|---|---|
| Purchase | 🖨 採購單 PO | 給供應商確認下單 |
| Sales | 🖨 銷售單 SO | 給客戶確認下單 |
| Sales（**已出貨**）| 📋 出貨單 DN | **跟貨走**，給司機 / 客戶簽收 |
| EInvoice（**剛開立**）| 🖨 電子發票 | 給客戶報帳用 |

點按鈕 → 跳 modal → 按「🖨 列印 / 存 PDF」→ 用瀏覽器原生 print → 「另存為 PDF」即可存檔。

格式含**標準台頭**（公司名 / 單據類型 / 單據號 / 日期）+ **三欄簽章區**（承辦人 / 主管 / 客戶供應商）。

### 3.14 ✅ 多階審批工作流 ✨ v3.22 新增

對標鼎新 / SAP B1 招牌功能。小工廠最常見情境：
- PO > NT$10 萬 → 老闆審
- SO 折扣 > 5% → 主管審
- 付款 > NT$5 萬 → 雙簽

#### 怎麼用

點 sidebar ✅ **審批**，3 個 tab：

1. **📋 待我審**：列出該我審的單（顯示申請人 / 金額 / 條件命中 / 階段）
   - 點 **[✓ 批准]** 直接過
   - 點 **[✗ 拒絕]** → 跳輸入框 → 必填原因
2. **🗂 歷史**：所有審過的紀錄 + 篩選
3. **⚙️ 規則設定**：建立 / 列出 / 停用規則
   - 範例：「PO 金額 > 100000 → 老闆審」

#### 自動觸發

PO/SO/付款建立後，系統**自動**評估規則：
- 命中 → 產 ApprovalRequest（pending）+ 通知該審的人
- 不命中 → 單據直接走下一步

> 💡 後台 EventBus 訂閱式設計：不會擋業務流程，**規則 evaluate 失敗只 log warning 不影響下單**。

### 3.15 📊 流程鏈視覺化 ✨ v3.22 新增

對標 SAP B1 Process Flow Chart。每張 PO / SO / WO row 加 📊 按鈕，點開看流程鏈：

```
採購流程 P2P 範例（PO-2026-0042 status=approved）：
✅ PO 建立 ─── ✅ 已核准 ─── 🔵 進貨中 ─── ⏳ 應付帳款 ─── ⏳ 付款
   5/15        老闆已核    [當前]         待 AP 入帳
```

- ✅ 綠 = 已完成
- 🔵 藍脈衝 = 當前
- ⏳ 灰 = 待處理
- ❌ 紅 = 取消

每張 PO/SO/WO 對應 5 步驟，**從 status 自動推導**（不用手動設）。

### 3.16 📝 單據備註 ✨ v3.22 新增

對標鼎新 / 正航 / SAP B1：每張單據可留 internal notes（給內部同事看，**不會印給客戶**）。

點 PO/SO/WO row 的 📝 按鈕 → modal → textarea 輸入備註 → 儲存。

範例用途：
- 業務：「客戶說週五前到，否則退單」
- 採購：「這家供應商長期合作可延付 30 天」
- 廠長：「林經理說這批可以加班趕，但要記不良率」

### 3.17 📋 Dashboard 待辦中心 ✨ v3.23 新增

對標鼎新 / SAP B1 Cockpit。每天登入第一眼看到的個人化提醒：

```
┌─ 📋 今日待辦中心 ────────────────────────────────┐
│  3        2         5             1              │
│  待我審    缺貨警示   草稿 PO 待核准  草稿 WO 待釋放 │
│  →         →         →              →             │
└─────────────────────────────────────────────────┘
```

每個格子可點擊 → 跳轉到對應頁。**沒待辦時整塊變綠色：「✅ 沒有待處理事項，做得好！」**

未來會加：逾期 AR / 超過 SLA 工單 / AI 觀察到的異常 etc.

### 3.18 🧬 BOM (做法 / Recipe) 物料表 ✨ v3.23 編輯器 + v3.25.9 多階 + AI

製造業核心。**沒做法工單無法 release**（real bug blocker）。

#### 怎麼用（前端編輯器）

1. 進「🏭 生產」頁
2. 點上方「🧬 管理 BOM」按鈕
3. 跳出產品清單 → 點要編的產品
4. 跳 BOM modal：
   - 顯示該產品現有的做法元件
   - 點「➕ 加元件」→ 選料件 + 填用量 → 儲存

#### 範例：簡單做法（單階）

產品「螺絲組 SCREW-SET-001」的做法：
| 元件料件 | 用量 (per 產品) | 單位 |
|---|---|---|
| M6 螺絲 | 1 | pcs |
| M6 螺帽 | 1 | pcs |
| 包裝盒 S | 1 | pcs |

→ 設好後該產品的 WO 可以 release，MRP 也會自動算需要拉多少料。

#### 範例：多階做法（v3.25.9 新增）

複雜產品常見「半成品 → 半成品 → 原料」的 2~3 階結構：

```
機器 MACHINE-001
├─ 螺絲組 SCREW-SET-001 × 4 組      (半成品，本身也是個 product)
│   ├─ M6 螺絲 × 1
│   ├─ M6 螺帽 × 1
│   └─ 包裝盒 S × 1
└─ 馬達 MOTOR-001 × 1
```

→ 跑 MRP 時，Ouvoca **自動遞迴展開**：1 台機器 = 4 個 M6 螺絲 + 4 個 M6 螺帽 + 4 個包裝盒 + 1 個馬達。

> 💡 **怎麼讓 Ouvoca 認得「半成品」？** 半成品需同時建在 Product 表（有自己的做法）和 Part 表（編號相同，如 `product_no == part_no == "SCREW-SET-001"`）。建議用「複製產品到 Part」的快捷功能（生產頁右上）。

#### 用 AI 對話管理做法（v3.25.9 新增）

3 個對話式工具（皆 ConfirmCard 二次確認）：
| 你說的話 | Ouvoca 做的事 |
|---|---|
| 「在 PROD-001 的做法加 M6 螺絲 4 個」 | `add_bom_item_with_confirm` → 跳卡確認 → 寫入 |
| 「把 PROD-001 的 M6 螺絲改成 6 個」 | `update_bom_item_with_confirm` → 跳卡確認 |
| 「從 PROD-001 的做法移除 M6 螺絲」 | `delete_bom_item_with_confirm` → 軟刪除（保留審計） |

### 3.19 📜 庫存異動歷史 ✨ v3.23 新增

進「📦 庫存」頁 → 點上方「📜 異動歷史」tab。

看到每筆庫存變動：
- 進貨（PO receive）→ `+50 M6 螺絲`
- 出貨（SO ship）→ `-10 螺絲組`
- 工單完工 → `+100 螺絲組`
- 盤點調整 → `+/- N`

可按料件過濾，看單一料件的全部進出歷史。倉管月底對帳就用這頁。

---

### 📌 §3.20–§3.34：v3.60–v3.69 新增功能（本段是新補的，前面章節沒寫過）

這一段補上 **v3.60 到 v3.69** 之間新增、但手冊前面完全沒提到的功能。開始看之前先認識三個標示：

| 標示 | 意思 | 你要怎麼做 |
|---|---|---|
| 🖱 **用點的** | 畫面上真的有按鈕 | 照步驟點就好 |
| 💬 **用講的** | **目前沒有畫面按鈕**，只能在「💬 AI 助手」打字 | 把本節的指令複製貼上，改成你的單號 / 數字 |
| 🔧 **要找 IT** | 目前既沒有按鈕也沒有 AI 指令，只有系統介面（API） | 把這一節截圖給 IT，請他幫你執行 |

每一節都照同一個順序寫：**在哪裡 → 情境 → 步驟 → 你會看到什麼 → 常見問題**。

> ⏱ **關於確認卡的倒數**：確認卡（ConfirmCard）目前預設的有效時間是 **30 分鐘**
> （程式設定在 `backend/app/agents/confirm_card.py` 的 `DEFAULT_TTL_SECONDS = 1800`）。
> 卡片上會直接顯示剩餘時間，**一律以你畫面上看到的數字為準**，不用自己算。

> 🔖 **單號長什麼樣？** 從 v3.60 起所有單號由系統統一發號（不會重號），格式是
> 「**類型-廠代碼-日期-流水號**」，例如 `PR-HQ-20260804-0001`、`GRN-HQ-20260804-0002`、`RFQ-HQ-20260804-0001`。
> 手冊裡寫的 `PR-2026-0031`、`PO-2026-0142` 都只是示意，**請以畫面上看到的單號為準**。

#### 這一段有哪些功能

| 節次 | 功能 | 主要入口 |
|---|---|---|
| §3.20 | 請購單 PR → 採購單 PO | 💬 用講的（核准那一步 🔧 要找 IT）|
| §3.21 | 收料（整批 / 分批）與收料單 GRN | 🖱 採購頁 +💬 用講的 |
| §3.22 | RFQ 詢價 → 比價 → 決標轉採購單 | 🖱 採購頁「📨 RFQ 詢價」分頁 |
| §3.23 | 領料單 MI（工單領料） | 💬 用講的 |
| §3.24 | 退貨單 RT（客戶退貨） | 💬 用講的（核准 / 入庫 🔧 要找 IT）|
| §3.25 | 條碼槍掃描 + 料件 QR 標籤列印 | 🖱 庫存頁 |
| §3.26 | 批號與序號正反向追溯 | 🖱 庫存頁 +💬 用講的 |
| §3.27 | 三大報表（試算表 / 損益表 / 資產負債表）| 💬 用講的 |
| §3.28 | 台灣 401/405 營業稅申報表 | 💬 用講的（另有 🖱 報表頁的 401 HTML）|
| §3.29 | 供應商發票 3-Way Match | 💬 用講的 |
| §3.30 | 固定資產與每月折舊 | 💬 用講的 |
| §3.31 | 銷貨成本月結（COGS 結轉）| 💬 用講的 |
| §3.32 | 雙重驗證 MFA | 🖱 設定頁 |
| §3.33 | 備份與還原 | 🖱 設定頁 |
| §3.34 | 系統組態 14 項 | 🖱 設定頁（6 項）+💬 用講的（其餘）|

---

### 3.20 📋 請購單 PR → 採購單 PO ✨ v3.63 新增

**這是什麼？** 現場發現缺料的人，先開一張「**請購單**」（單號 `PR-…`）說「我要什麼、幾個、什麼時候要」，
主管核准後，採購再決定「跟哪一家買、多少錢」，把它轉成正式的**採購單**（`PO-…`）。

> 白話：**請購單 = 我要買**；**採購單 = 我真的下單了**。中間卡一道主管核准，避免現場自己亂訂料。

> 📍 **在哪裡**：💬 建立與轉單：AI 助手；🔧 核准：目前只能找 IT

#### 📖 情境

阿明在產線發現 M6 螺絲快沒了，但他沒有下採購單的權限。他要先開請購單。

#### 🖱 步驟

**第 1 步：建立請購單（💬 用講的）**

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「💬 AI 助手」 |
| 2 | 打字：`請購 100 個 M6-BOLT-20，8/20 要` |
| 3 | 跳出確認卡，核對**料號、數量、需求日** |
| 4 | 點「✓ 確認執行」 |

**第 2 步：主管核准（🔧 目前要找 IT）**

⚠️ 誠實告知：**「核准請購單」這一步目前既沒有畫面按鈕，也沒有 AI 指令。**
需要請 IT 呼叫 `POST /api/purchase/requisitions/{pr_id}/approve`（權限碼 `purchase.pr.approve`）。
沒有核准，第 3 步一定會被擋。

**第 3 步：轉成採購單（💬 用講的）**

| 步驟 | 操作 |
|---|---|
| 1 | 在「💬 AI 助手」打字：`把 PR-2026-0031 轉成給長江精密的採購單` |
| 2 | 確認卡會顯示：請購單號 + 狀態、供應商、以及「品項與數量依請購單帶入，單價以料件成本為預設」 |
| 3 | 點「✓ 確認執行」 |
| 4 | **轉完一定要去「🛒 採購」頁核對單價**（見下方常見問題）|

#### 👀 你會看到什麼

```
┌────────────────────────────────────────────────────┐
│  📋 請購單 PR-2026-0031 轉採購單                 │
├────────────────────────────────────────────────────┤
│  請購單：PR-2026-0031（狀態 approved）           │
│  供應商：長江精密工業（SUP-001）                    │
│  品項與數量依請購單帶入，單價以料件成本為預設        │
├────────────────────────────────────────────────────┤
│              [ 取消 ]      [ ✓ 確認執行 ]           │
└────────────────────────────────────────────────────┘
```

轉單成功後：

- 產生一張新的採購單，備註自動寫上「**由請購單 PR-2026-0031 轉入**」
- 請購單狀態變成 `converted`（已轉單），**不能再轉第二次**
- 採購單的交期沿用請購單上的需求日

#### 三種狀態的意思

| 狀態 | 中文 | 代表 |
|---|---|---|
| `draft` | 草稿 | 剛開好，還沒人核准 |
| `approved` | 已核准 | 主管准了，可以轉採購單 |
| `converted` | 已轉單 | 已經變成採購單，這張請購單的任務結束 |

#### ❓ 常見問題

| 你看到的訊息 | 意思 | 你該做什麼 |
|---|---|---|
| 「找不到料件 'M6 螺絲'」 | 講的品名對不上料號 | 去「📦 庫存」頁抄正確的**料號**（例 `M6-BOLT-20`）再講一次 |
| 「請購單尚未核准，不可轉採購。」 | 這是**家規**擋的，PR 還是草稿 | 請主管核准（目前要找 IT，見第 2 步）|
| 「此請購單已轉成採購單」 | 同一張 PR 只能轉一次 | 去採購頁找那張已經產生的 PO，不要再轉 |
| 轉出來的單價是 0 或很奇怪 | 轉單時單價是抓**料件的「單位成本」**，不是跟供應商談的價 | 到「📦 庫存」頁把該料件的「單位成本」填好，或轉完後請採購改單價 |

---

### 3.21 🚚 收料：整批收 / 只收一部分 / 收料單 GRN ✨ v3.63 新增

**這是什麼？** 供應商送貨來了，你要在系統上「收料」，庫存才會加上去。
Ouvoca 有兩條路：畫面上一鍵**整批全收**，以及用 AI 對話處理**只收到一部分**的狀況。

> 📍 **在哪裡**：🖱 整批：左側「🛒 採購」→「📋 採購單」分頁 → 該列的「🚚 進貨」<br>💬 分批：AI 助手

#### 📖 情境

阿玲下了 PO-2026-0142（M6 螺絲 2000 個）。今天貨到了。

#### 🖱 步驟 A：整批全收（最常見）

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「🛒 採購」 |
| 2 | 確認在上方「📋 採購單」分頁（旁邊還有「🏭 供應商」「📨 RFQ 詢價」）|
| 3 | 在列表找到 `PO-2026-0142` |
| 4 | 點該列右邊綠色的 **「🚚 進貨」** 按鈕 |
| 5 | 瀏覽器跳出提示：**「進貨：PO-2026-0142 全部訂購量都收到？ 部分收貨請用 AI 對話。」** |
| 6 | 點「確定」 |

> 💡 **看不到「🚚 進貨」按鈕？** 這顆按鈕只在採購單狀態是 **已核准 (approved) / 已發送 (sent) / 部分收貨 (partial_received)** 時才出現。
> 如果單子還是「草稿」，請先點旁邊的 **「✓ 核准」**。

#### 👀 你會看到什麼

- 跳出 **「✅ 進貨完成：3 項」**（數字 = 這次收了幾個品項）
- 採購單狀態變成 **已收貨 (received)**
- 到「📦 庫存」頁 →「📜 異動歷史」分頁，會看到每個料件一筆 **➕ 入庫**，來源單據寫 `purchase_order`

#### 🖱 步驟 B：只收到一部分（💬 用講的）

畫面上做不到——系統自己的提示就寫著「部分收貨請用 AI 對話」。

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「💬 AI 助手」 |
| 2 | 打字：`收貨 PO-2026-0142`（AI 會列出每一項要收多少）|
| 3 | **仔細核對確認卡上每一項的數量** |
| 4 | 數量不對 → 點「取消」，改講一次：`PO-2026-0142 這次 M6-BOLT-20 只收到 300 個` |
| 5 | 數量正確才點「✓ 確認執行」 |

> ⚠️ **一定要核對數字**：如果你沒有明講實收數量，系統預設會用「**訂購量全部**」帶進確認卡。
> 這正是確認卡存在的意義——**卡片上的數字才算數，不是你講的話**。

收了一部分後，採購單狀態會變成 **部分收貨 (partial_received)**，之後還可以再收剩下的。

#### 📄 收料單 GRN 是什麼？

v3.63 起，系統多了一種正式的收料單據 **GRN**（單號 `GRN-…`），一次做三件事：
**① 開一張收料單 ② 更新採購單的已收量 ③ 把料入庫**，三件事同時成功或同時失敗。

- 目前**沒有畫面按鈕**，查詢與開單都是系統介面：`GET /api/purchase/grns`、`POST /api/purchase/grns`（權限 `purchase.grn.read` / `purchase.grn.create`）
- GRN 有**超收防呆**：累計收貨量超過訂購量會直接被擋下

<details>
<summary>🔧 給 IT：GRN 對話指令目前有已知缺陷</summary>

AI 工具 `receive_grn_with_confirm`（`backend/app/agents/domains/m3_tools.py:120`）組出的收貨清單欄位名是 `po_item_id`，
但 `backend/app/services/m3_documents.py:163` 的 `create_grn()` 讀的是 `r["item_id"]`，按下確認後會拋 `KeyError`。
在修好之前，請使用者走採購頁「🚚 進貨」或 `receive_purchase_order_with_confirm`。

</details>

#### ❓ 常見問題

| 你看到的訊息 | 意思 | 你該做什麼 |
|---|---|---|
| 「此 PO 尚未核准，不可收貨。」 | **家規**擋下（收貨需 PO 已核准）| 先點該列的「✓ 核准」；權限不足就找主管 |
| 「所有項目已收齊」 | 這張單早就收完了 | 不用再收，確認是不是拿錯單號 |
| 「超收：PO 行 … 超過訂購量 …」 | 你要收的數量比訂的還多 | 核對送貨單；真的多送要請採購先改單 |
| 收完庫存沒變 | 通常是看錯料件或還沒重新整理 | 去「📦 庫存」→「📜 異動歷史」用料件過濾確認 |

---

### 3.22 📨 RFQ 詢價 → 比價 → 決標轉採購單 ✨ v3.64 新增

**這是什麼？** 買東西前先問三家價錢（詢價），把各家報價打進系統，並排比一比（比價），
選定最便宜／最合適的那家（決標），系統**自動幫你開好採購單**。

> 📍 **在哪裡**：🖱 左側「🛒 採購」→ 右上「📨 RFQ 詢價」分頁

#### 📖 情境

阿玲要買 1000 個 M6 螺絲，想先問長江、大華、台鋼三家的價錢再決定。

#### 🖱 步驟

**第 1 步：建立詢價單**

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「🛒 採購」 |
| 2 | 點右上的 **「📨 RFQ 詢價」** 分頁 |
| 3 | 點右邊的 **「＋ 建立詢價單」** 按鈕（點一下會展開表單，再點一次會變成「取消」收起來）|
| 4 | 「料件」下拉選 `M6-BOLT-20`，「數量」填 `1000` |
| 5 | 點綠色 **「建立」** |

你會看到綠字 **「✅ RFQ 已建立」**，下方表格出現一列，狀態 `draft`。

**第 2 步：送出詢價**

| 步驟 | 操作 |
|---|---|
| 1 | 在詢價單列表找到剛剛那一列 |
| 2 | 點藍色 **「送出」** 按鈕 |
| 3 | 狀態變成 `sent`，出現「已送出詢價」 |

> ⚠️ **「送出」不會自動寄信給供應商！** 它只是把單子的狀態改成「已送出」，讓你可以開始登錄報價。
> 實際的詢價還是要你自己打電話、傳 LINE 或寄 Email 給那三家。

**第 3 步：登錄各家報價**

狀態變成 `sent` 之後，該列才會出現「登錄報價」和「比價」兩顆按鈕。

| 步驟 | 操作 |
|---|---|
| 1 | 點綠色 **「登錄報價」** |
| 2 | 下方展開表單：「供應商」下拉選長江，「單價」填 `5` |
| 3 | 點 **「登錄」** → 出現「✅ 報價已登錄」 |
| 4 | **重複 1–3**，把大華、台鋼的報價也打進去 |

> 💡 畫面上的登錄表單一次只處理**詢價單的第一個項目**。
> 如果一張詢價單有很多料件，請改用 AI 對話：`長江對 RFQ-… 報價：M6-BOLT-20 1000 個每個 5 元、M6-NUT 1000 個每個 2 元`

**第 4 步：比價**

點該列的 **「比價」** → 下方出現「比價結果：RFQ-…」表格：

```
┌──────────────────────────────────────────────┐
│  比價結果：RFQ-2026-0007          ✕ 關閉  │
├──────────────┬──────────────┬────────────────┤
│  報價         │        金額   │        操作     │
├──────────────┼──────────────┼────────────────┤
│  SUP-001     │        5,000  │     [ 決標 ]   │
│  SUP-003     │        5,400  │     [ 決標 ]   │
│  SUP-002     │        6,200  │     [ 決標 ]   │
└──────────────┴──────────────┴────────────────┘
        ↑ 由便宜到貴自動排序，第一列就是最低價
```

**第 5 步：決標（系統自動開採購單）**

| 步驟 | 操作 |
|---|---|
| 1 | 在比價表點你要的那一列的紅色 **「決標」** |
| 2 | 跳出確認：**「決標後將自動轉採購單，確定？」** |
| 3 | 點「確定」 |

#### 👀 你會看到什麼

- 綠字 **「✅ 已決標並轉成 PO-2026-0158」**
- 「📋 採購單」分頁多一張新採購單，備註寫著「**由 RFQ-2026-0007 決標轉入**」，單價就是得標的報價
- 沒得標的報價狀態變成 `declined`（未得標），詢價單狀態變成 `awarded`

#### 💬 用講的版本（多項目、比較複雜時用）

| 你說的話 | 系統做的事 |
|---|---|
| `針對 M6-BOLT-20 1000 個發詢價` | 建立詢價單 |
| `長江對 RFQ-… 報價 M6-BOLT-20 1000 個每個 5 元，交期 7 天` | 登錄報價 |
| `RFQ-… 誰最便宜` | 比價（含每個料件的最低價）|
| `RFQ-… 給長江得標` | 決標並自動轉採購單 |

#### ❓ 常見問題

| 症狀 | 意思 | 你該做什麼 |
|---|---|---|
| 只有「送出」沒有「登錄報價」 | 單子還是 `draft` | 先點「送出」 |
| 「詢價單尚未送出，不可決標。」 | **家規**擋的 | 同上，先送出 |
| 比價表的「報價」欄看到一串英數字 | 那是**供應商代碼**不是名稱 | 到「🏭 供應商」分頁對照代碼 |
| 決標後採購單沒出現 | 列表還沒刷新 | 切回「📋 採購單」分頁，或按 F5 |

---

### 3.23 🧾 領料單 MI：工單從倉庫領料 ✨ v3.63 新增

**這是什麼？** 工單開始做之前要去倉庫拿料。用「領料單」（`MI-…`）登記，
系統會**扣掉原料庫存**，並且把「當時的成本」記下來——之後才算得出這張工單到底賺不賺錢。

> 📍 **在哪裡**：💬 只能用講的（目前沒有畫面按鈕）

#### 📖 情境

WO-2026-0091 已經釋放到產線，領班要去倉庫領 100 個 M6 螺絲。

#### 🖱 步驟

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「💬 AI 助手」 |
| 2 | 打字：`WO-2026-0091 領 100 個 M6-BOLT-20` |
| 3 | 確認卡會顯示工單號、每一項料號 × 數量、以及「扣原料庫存 + 領料成本快照」 |
| 4 | 點「✓ 確認執行」 |

#### 👀 你會看到什麼

- 產生領料單號 `MI-…`，狀態 `posted`（已過帳）
- 「📦 庫存」→「📜 異動歷史」出現 **➖ 出庫**，來源單據 `material_issue`
- 之後在 AI 助手問 `WO-2026-0091 這張單是賺是賠`，材料成本就會把這次領的料算進去

#### ❓ 常見問題

| 你看到的訊息 | 意思 | 你該做什麼 |
|---|---|---|
| 「工單尚未釋放，不可領料。」 | **家規**擋的：只有「已釋放 / 生產中」的工單能領料 | 去「🏭 生產」頁點該工單的 **「▶ 釋放」** |
| 「找不到料件『螺絲』」 | 要講**料號**不是俗名 | 去庫存頁抄料號 |
| 「領料數量必須大於 0」 | 數量填成 0 或負數 | 重講一次 |
| 領了料但工單成本還是 0 | 該料件的「單位成本」是 0 | 到「📦 庫存」頁把單位成本填好（成本是**領料當下**抓的，之後改不會回溯）|

> 💡 系統**不會**自動照「做法 (Recipe)」幫你算要領多少——你講幾個就領幾個。
> 想知道該領多少，先在 AI 助手問：`PROD-001 的做法有哪些料件`。

---

### 3.24 ↩️ 退貨單 RT：客戶退貨回來 ✨ v3.63 新增

**這是什麼？** 客戶把貨退回來，開一張「退貨單」（`RT-…`）。
**核准之後**才能入庫，庫存才會加回去——避免有人隨手把退貨塞回庫存。

> 📍 **在哪裡**：💬 建立：AI 助手；🔧 核准 + 入庫：目前只能找 IT

#### 📖 情境

大同公司退回 10 個 M6 螺絲，說是品質不良。

#### 🖱 步驟

**第 1 步：建立退貨單（💬 用講的）**

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「💬 AI 助手」 |
| 2 | 打字：`大同公司退回 10 個 M6-BOLT-20，品質不良` |
| 3 | 確認卡顯示客戶、品項 × 數量、退貨原因 |
| 4 | 點「✓ 確認執行」 → 產生 `RT-…`，狀態 `draft` |

**第 2 步：核准 → 第 3 步：退貨入庫（🔧 目前要找 IT）**

⚠️ 誠實告知：這兩步目前**沒有畫面按鈕也沒有 AI 指令**，需要 IT 呼叫：

- 核准：`POST /api/sales/returns/{return_id}/approve`（權限 `sales.return.approve`）
- 入庫：`POST /api/sales/returns/{return_id}/process`（權限 `sales.return.process`）
- 查清單：`GET /api/sales/returns`（權限 `sales.return.read`）

#### 👀 你會看到什麼

入庫完成後：

- 退貨單狀態變 `processed`（已處理）
- 「📦 庫存」→「📜 異動歷史」出現 **➕ 入庫**，來源單據 `return_note`，備註 `RT-… 退貨入庫`

#### 三種狀態的意思

| 狀態 | 中文 | 代表 |
|---|---|---|
| `draft` | 草稿 | 已登記，還沒人核准，**庫存還沒動** |
| `approved` | 已核准 | 主管准了，可以入庫 |
| `processed` | 已入庫 | 庫存已經加回去，流程結束 |

#### ❓ 常見問題

| 你看到的訊息 | 意思 | 你該做什麼 |
|---|---|---|
| 「退貨單尚未核准，不可入庫。」 | **家規**擋的 | 先完成核准那一步 |
| 「找不到客戶『大同』」 | 客戶名稱對不上 | 去「💰 銷售」頁的客戶列表抄正確名稱或代碼 |
| 退了貨但庫存沒加回來 | 停在 `draft` 或 `approved`，還沒真的入庫 | 請 IT 執行第 3 步 |

---

### 3.25 🔫 條碼槍掃描 + 🏷️ 料件 QR 標籤列印 ✨ v3.64 新增

**這是什麼？** 倉庫貼標籤、用條碼槍嗶一下就查到這是什麼料、還有多少庫存，
以及把料號印成 QR 標籤貼在料架上。

> 📍 **在哪裡**：🖱 左側「📦 庫存」頁**最上方**那一條有 🔫 圖示的橫列（兩個分頁都看得到）

#### 📖 情境一：嗶一聲查料

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「📦 庫存」 |
| 2 | 游標本來就停在最上方那個框（寫著「掃描 / 輸入料號、批號或序號…」）|
| 3 | 把 USB 條碼槍插上電腦，對著標籤 **「嗶」** 一聲（沒有槍就用鍵盤打料號）|
| 4 | 按 **Enter**，或點右邊藍色 **「掃描」** 按鈕 |

#### 👀 你會看到什麼

框框下面出現一排結果：

```
┌────────────────────────────────────────────────────────────┐
│ 🔫 [ M6-BOLT-20                          ]      [ 掃描 ]   │
├────────────────────────────────────────────────────────────┤
│  類型        代碼            名稱            庫存           │
│  料件        M6-BOLT-20      M6 六角螺絲     1,240          │
│                                                             │
│  [ 🏷️ 列印標籤 ]                                            │
└────────────────────────────────────────────────────────────┘
```

系統會自己判斷你掃到的是**料件 / 批號 / 序號**三種的哪一種：

| 掃到的是 | 會顯示 | 會出現的按鈕 |
|---|---|---|
| 料件（料號）| 名稱、現有庫存 | **「🏷️ 列印標籤」** |
| 批號 | 批號、數量、狀態 | **「🔍 追溯」**（見 §3.26）|
| 序號 | 序號、狀態 | **「🔍 追溯」** |

#### 📖 情境二：印 QR 標籤貼料架

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「📦 庫存」，確認在「📦 料件」分頁 |
| 2 | 在料件表格找到那一列，最右邊「操作」欄有一顆黑色 **🏷️** 按鈕 |
| 3 | 點下去 |
| 4 | 瀏覽器自動下載檔案 **`label-M6-BOLT-20.pdf`** |
| 5 | 打開 PDF → 用標籤機或一般印表機印出來貼上去 |

標籤長這樣（**60 × 40 公釐**）：

```
┌───────────────────────────────┐
│ M6-BOLT-20                    │   ← 料號（大字）
│ M6 Hex Bolt                   │   ← 名稱
│                    ┌────────┐ │
│                    │ ▓▓ ▓▓▓ │ │   ← QR（內容就是料號）
│                    │ ▓▓▓  ▓ │ │      掃它會回到上面的查詢
│                    └────────┘ │
└───────────────────────────────┘
```

#### ❓ 常見問題

| 症狀 | 意思 | 你該做什麼 |
|---|---|---|
| 「找不到對應料號/批號/序號：XXX」 | 這個碼系統裡沒有 | 確認料號有沒有打錯；批號 / 序號要先建檔（見 §3.26）|
| 條碼槍嗶完沒反應 | 條碼槍沒設定成「掃完自動按 Enter」 | 照條碼槍說明書設定「Enter / CR 後綴」，或自己按一下 Enter |
| 點 🏷️ 沒下載 | 瀏覽器擋下載 | 看網址列右邊有沒有被擋的提示，選「允許」 |
| 標籤上的中文品名印不出來或變亂碼 | 標籤用的是英數字型 | 料號請用英數字；中文品名可能顯示不正常，這是已知限制 |
| 點 🏷️ 跳出「列印失敗」 | 伺服器缺少產生 QR 的元件 | 找 IT：後端要安裝 `qrcode` 套件 |

---

### 3.26 🔍 批號與序號正反向追溯 ✨ v3.64 新增

**這是什麼？** 客訴來了，你要能在 3 分鐘內回答兩個問題：

| 問題 | 業界叫法 | Ouvoca 怎麼看 |
|---|---|---|
| 「這批料是**從哪來**的？」 | 反向追溯 | 看**流入 (in)** 的紀錄：收料、進貨、退貨入庫 |
| 「這批料**被哪些單據用掉**了？」 | 正向追溯 | 看**流出 (out)** 的紀錄：領料、出貨 |

> 📍 **在哪裡**：🖱 庫存頁掃描列的「🔍 追溯」；💬 完整清單用 AI 助手

#### 🖱 步驟一：先建批號 / 序號（💬 用講的）

沒有先建檔，就沒有東西可以追。

| 你說的話 | 系統做的事 |
|---|---|
| `料件 M6-BOLT-20 建批號 LOT-20260801，100 個，2026-10-01 到期` | 建立批號主檔 |
| `登記 SN-0001 到 SN-0010 這 10 個序號到批號 LOT-20260801` | 批量登記序號（**序號重複會被拒絕**）|

#### 🖱 步驟二：追溯（畫面版）

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「📦 庫存」 |
| 2 | 在最上方 🔫 掃描框輸入（或用條碼槍掃）批號 `LOT-20260801` |
| 3 | 按 Enter |
| 4 | 結果列出現後，點藍紫色 **「🔍 追溯」** 按鈕 |

#### 👀 你會看到什麼

畫面上會出現一行藍字摘要，例如：

```
批號 LOT-20260801：流入 2 筆 / 流出 5 筆動向
```

- **流入 2 筆** = 這批料從 2 張單據進來的（反向追溯）
- **流出 5 筆** = 這批料被 5 張單據用掉了（正向追溯）

查序號則會顯示：`序號 SN-000123：in_stock，最近文件 WO-2026-0091`

#### 💬 想看「每一筆是哪張單」就用講的

畫面只給你數量摘要，**明細要用 AI 助手**：

| 你說的話 | 你會拿到 |
|---|---|
| `追溯 LOT-20260801` | 每一筆動向：方向（流入/流出）、數量、**單據號**（PO / GRN / WO / MI / 出貨單 / 退貨單）、時間 |
| `查序號 SN-000123` | 序號狀態、所屬批號、最近經手的文件 |

#### ❓ 常見問題

| 症狀 | 意思 | 你該做什麼 |
|---|---|---|
| 「批號不存在：LOT-…」 | 沒建過這個批號 | 先用上面的指令建批號 |
| 「序號 SN-0001 已存在」 | 序號不能重複 | 換一個序號，或先查那個序號現在在哪 |
| 追溯結果是空的 | 追溯是靠**庫存異動上的批號欄位**串起來的 | 收料 / 領料時如果沒有帶批號，那筆異動就不會出現在追溯裡——現場作業要養成「收發料都報批號」的習慣 |

---

### 3.27 📊 三大報表：試算表 / 損益表 / 資產負債表 ✨ v3.61 新增

**這是什麼？** 會計最基本的三張表。資料來源是**已過帳 (posted) 的傳票**——草稿傳票不算。

> 📍 **在哪裡**：💬 **只能用講的**。「📒 會計」頁目前只有「📊 傳票 / 💵 應收帳款 / 📚 科目表」三個分頁，沒有這三張報表

#### 📖 三張表各在回答什麼問題

| 報表 | 白話 | 你該看哪個數字 |
|---|---|---|
| **試算表**（Trial Balance）| 「帳有沒有記錯？」| `balanced`：借方總額 = 貸方總額 就是 ✅ 平衡 |
| **損益表**（Income Statement）| 「這段期間**賺了還是賠了**？」| `net_income` 本期淨利 |
| **資產負債表**（Balance Sheet）| 「公司**現在的家底**有多少？」| `total_asset` 總資產 = `total_liability` 總負債 + `total_equity` 總權益 |

#### 🖱 步驟

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「💬 AI 助手」 |
| 2 | 打字（**期間格式很重要，見下方警告**）：|
| | `試算表平不平衡`（不指定期間 = 全部期間）|
| | `給我 2026-08 的損益表` |
| | `給我 2026-08 的資產負債表` |

> ⚠️ **期間要打對，不然查不到東西**：
> 三大報表的期間格式是 **`2026-08`（中間有短橫線）**，因為傳票上的期間就是這樣存的。
> §3.28 的 401/405 則是 **`202608`（六碼、沒有橫線）**。兩個不一樣，別搞混。

#### 👀 你會看到什麼（損益表為例）

```
期間：2026-08
營業收入 (revenue)            4,820,000
營業成本 (cost)             − 3,692,000
─────────────────────────────────────
營業毛利 (gross_profit)       1,128,000   ← 收入 − 成本
營業費用 (expense)          −   612,000
─────────────────────────────────────
營業利益 (operating_income)     516,000   ← 毛利 − 費用
業外收入 / 支出 / 所得稅       ± …
─────────────────────────────────────
本期淨利 (net_income)           480,000   ← 這就是「這個月賺多少」
```

資產負債表的**權益**裡會**包含本期淨利**（還沒結轉到保留盈餘）。

#### ❓ 常見問題

| 症狀 | 意思 | 你該做什麼 |
|---|---|---|
| 報表全部是 0 | 傳票還是「草稿」沒過帳 | 到「📒 會計」→「📊 傳票」分頁，把該過的傳票點 **「✓ 過帳」** |
| 試算表 `balanced` 是 false | 借貸不平 | 找會計檢查傳票；家規本來就會擋不平衡的傳票，出現這種狀況要查是不是有人繞過 |
| 損益表數字比想像中少 | 出貨的成本還沒結轉 | 先做 §3.31 的「銷貨成本月結」 |
| 某個科目沒出現在報表上 | 科目沒有設定「財報歸屬 (fs_line)」 | 找 IT 檢查科目表；用系統內建的 86 科通常不會有這問題 |

---

### 3.28 🧾 台灣 401 / 405 營業稅申報表 ✨ v3.61 新增

**這是什麼？** 每兩個月要跟國稅局申報的營業稅。系統幫你把數字算好：

| 代號 | 白話 | 資料從哪來 |
|---|---|---|
| **401** | **銷項**：你賣出去、開了多少發票、收了多少稅 | 期間內**已開立且沒作廢**的電子發票 |
| **405** | **進項**：你進貨、拿到多少發票、付了多少稅 | 期間內的**應付帳款（供應商發票）** |
| **應納稅額** | 銷項稅 − 進項稅 = 這期要繳多少 | 系統自動相減 |

> 📍 **在哪裡**：💬 AI 助手（數字）；🖱 報表頁的「🧾 台灣 401 報表 HTML」（可列印的申報書格式，見 §3.10）

#### 🖱 步驟

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「💬 AI 助手」 |
| 2 | 打字：`查 202608 的 401 405 營業稅` |

> ⚠️ 這裡的期間是 **`202608`（六碼、沒有橫線）**，跟 §3.27 三大報表的 `2026-08` 不一樣。

#### 👀 你會看到什麼

```
期間 202608　稅率 5%
─────────────────────────────────
401 銷項
  一般銷售額      2,400,000
  銷項稅額          120,000
  發票張數               38
  零稅率 / 免稅 / 外銷      0     ← 目前固定為 0，見下方說明
─────────────────────────────────
405 進項
  一般進貨額      1,600,000
  進項稅額           80,000
  發票張數               21
─────────────────────────────────
應納稅額            40,000        ← 120,000 − 80,000
```

#### ❓ 常見問題

| 症狀 | 意思 | 你該做什麼 |
|---|---|---|
| 401 金額比實際少 | 只算**已開立電子發票**的部分 | 出貨後要記得開發票（「🧾 電子發票」頁）；作廢的發票不會算進去 |
| 零稅率 / 免稅 / 外銷都是 0 | 目前系統只處理一般應稅 | 有外銷業務請自行加計，或找 IT 評估 |
| 405 數字和帳上對不起來 | 舊資料如果只有「含稅總額」，系統會用 5% 回推拆算 | 之後登記供應商發票時，把未稅金額和稅額分開填（見 §3.29）|
| 要交給國稅局的紙本 | AI 給的是數字，不是申報書格式 | 去「📈 報表」頁開「🧾 台灣 401 報表 HTML」，按 Ctrl+P 印或存 PDF |

---

### 3.29 🔗 供應商發票 3-Way Match（三方勾稽）✨ v3.61 新增

**這是什麼？** 付錢給供應商之前，系統幫你自動比對三份文件：

```
        採購單 PO          收貨紀錄          供應商發票
      「我訂了幾個」  ↔  「我實際收到幾個」 ↔ 「他跟我收幾個的錢」
             ↘              ↓               ↙
                     三個對得起來才付錢
```

這就是「3-Way Match」。抓的是最容易吃虧的兩種狀況：**收到 300 卻請款 500**、**談好 5 元卻開 6 元**。

> 📍 **在哪裡**：💬 只能用講的（會計頁沒有這個分頁）

#### 🖱 步驟

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「💬 AI 助手」 |
| 2 | 登記發票：`記錄長江精密的發票 INV-100，對應 PO-2026-0142，M6-BOLT-20 1000 個每個 5 元` |
| 3 | 確認卡會顯示供應商、發票號、銷售額、稅額（沒填就自動抓 5%）、行數、關聯的 PO |
| 4 | 點「✓ 確認執行」 → **建立完會自動跑一次勾稽** |
| 5 | 之後隨時查：`哪些供應商發票數量對不上`（可過濾 `qty_variance`）|

#### 👀 你會看到什麼：四種狀態

| 狀態 | 中文 | 意思 | 你該怎麼處理 |
|---|---|---|---|
| `matched` | ✅ 完全相符 | 數量、單價都對得上 | 可以安心付款 |
| `qty_variance` | ⚠️ **數量差異** | 發票數量 ≠ **實際收貨量** | 先確認是不是還沒收完（`partial_received`）；真的少收就請供應商改開發票 |
| `price_variance` | ⚠️ **單價差異** | 數量對，但單價 ≠ 採購單上的價 | 對照當初的報價 / 決標紀錄；是漲價就請採購先改採購單 |
| `unmatched` | ❌ **對不上** | 發票沒有關聯採購單，或找不到對應的採購單行 | 補上 PO 單號重登；沒有 PO 的採購要問清楚是誰買的 |

系統會告訴你差多少，例如：

```
數量差 -200（發票 1000 vs 收貨 800）
單價差 1（發票 6 vs PO 5）
```

> 💡 差異在 **0.005 以內**視為相同（避免四捨五入造成假警報）。
> 一張發票只要有**任何一行**對不上，整張發票就會被標成該狀態，優先序是：unmatched > 數量差 > 單價差。

#### ❓ 常見問題

| 症狀 | 意思 | 你該做什麼 |
|---|---|---|
| 剛收到貨就被標 `qty_variance` | 系統比的是「**已收貨量**」不是「訂購量」 | 先完成收料（§3.21）再登發票 |
| 「找不到供應商 xx 的 PO PO-…」 | 那張 PO 不屬於這家供應商 | 核對單號和供應商是不是對錯人 |
| 所有發票都是 `unmatched` | 登記發票時沒填 PO 單號 | 重登時務必講「對應 PO-…」 |

---

### 3.30 🏭 固定資產與每月折舊 ✨ v3.61 新增

**這是什麼？** 買了一台一百萬的 CNC，不能當成「這個月花了一百萬」——要分好幾年慢慢攤。
這個攤法叫**折舊**，Ouvoca 用最單純的**直線法**：

```
每月折舊 = (取得成本 − 殘值) ÷ 耐用月數
例：(1,000,000 − 100,000) ÷ 60 個月 = 每月 15,000
```

> 📍 **在哪裡**：💬 只能用講的

#### 🖱 步驟

| 步驟 | 操作 |
|---|---|
| 1 | **登記資產**：在 AI 助手打 `新增固定資產 CNC 車床，成本 100 萬，殘值 10 萬，耐用 60 個月，類別 machinery` |
| 2 | 確認卡會顯示名稱、成本 / 殘值 / 耐用月數、類別 → 點「✓ 確認執行」 |
| 3 | **每個月提折舊**：打 `幫 CNC 車床提 2026-08 的折舊` |
| 4 | 確認卡顯示月折舊金額、目前累計折舊，以及會產生的傳票 → 點「✓ 確認執行」 |
| 5 | **隨時查**：打 `機器設備現在帳面價值多少` |

#### 👀 你會看到什麼

提折舊會自動產生並過帳一張傳票：

```
借 (DR)  6310 折舊費用          15,000
貸 (CR)  1631 累計折舊-機器設備  15,000
```

累計折舊科目會依資產類別自動選：

| 類別 | 中文 | 累計折舊科目 |
|---|---|---|
| `machinery` | 機器設備 | 1631 |
| `building` | 房屋及建築 | 1621 |
| `vehicle` | 運輸設備 | 1641 |
| `furniture` | 辦公設備 | 1651 |

查詢時每一筆資產會顯示：成本、殘值、耐用月數、月折舊、累計折舊、**帳面價值 (net_book_value)** 和狀態。

#### ❓ 常見問題

| 你看到的訊息 | 意思 | 你該做什麼 |
|---|---|---|
| 「資產 FA-… 已於 2026-08 提列折舊（傳票 …），請勿重複」 | 防呆：同一個月只能提一次 | 確認是不是已經提過了；要提別的月份就改期間 |
| 「已提滿折舊」/ 狀態 `fully_depreciated` | 累計折舊已經到達（成本 − 殘值）| 正常現象，這台機器不用再提了 |
| 「折舊科目不存在（請先 seed_accounts）」 | 科目表沒有 6310 或 1631 | 找 IT 執行會計科目種子（系統內建 86 科）|
| 最後一個月金額比較小 | 系統會自動只提到「剛好提滿」為止 | 這是正確的，不用調整 |

---

### 3.31 📦➡️💰 銷貨成本月結（COGS 結轉）✨ v3.61 新增

**這是什麼？** 這個月出貨出去的東西，**成本**要從「製成品（庫存）」轉到「銷貨成本（費用）」，
損益表上的「營業成本」才會有數字，毛利才算得準。

> 📍 **在哪裡**：💬 只能用講的

#### 🖱 步驟

| 步驟 | 操作 |
|---|---|
| 1 | 確認這個月的出貨都做完了（「💰 銷售」頁該出的都按過「📦 出貨」）|
| 2 | 點左側「💬 AI 助手」 |
| 3 | 打字：`結 2026-08 的銷貨成本` |
| 4 | 確認卡會顯示期間、計算方式、要產生的傳票，以及一行紅字警告 |
| 5 | 點「✓ 確認執行」 |

#### 👀 你會看到什麼

確認卡上會寫：

```
期間：2026-08
計算方式：期間內出貨明細 × Part.unit_cost
產生傳票：DR 5100 銷貨成本 / CR 1340 製成品
⚠️ 重複執行會產生多張結轉傳票（結帳前請確認）
```

執行後會回報這期的總銷貨成本和每個料件的明細。

> ⚠️ **一個月只做一次**。系統雖然有防重複的檢查（同期間已結轉會擋下並告訴你傳票號），
> 但這是每月結帳的固定動作，建議固定由會計一個人做，做完在 §3.27 看損益表對數字。

#### ❓ 常見問題

| 症狀 | 意思 | 你該做什麼 |
|---|---|---|
| 「期間 2026-08 已結轉銷貨成本（傳票 …），請勿重複」 | 這個月已經結過了 | 不用再結；要看結果就查那張傳票 |
| 回報「期間內沒有出貨紀錄」 | 這個月沒有出貨單 | 確認出貨有沒有真的按下去（狀態要是「已出貨」）|
| 結出來的成本是 0 | 料件的「單位成本」沒填 | 到「📦 庫存」頁補上單位成本，成本是用**目前**的單位成本算的 |
| 期間格式 | 這裡 `2026-08` 和 `202608` 都吃得下 | 建議統一用 `2026-08` |

---

### 3.32 🔐 雙重驗證 MFA（手機驗證碼）✨ v3.64 新增

**這是什麼？** 開啟之後，登入除了密碼，還要輸入手機 App 上每 30 秒換一次的 **6 位數字**。
就算密碼被別人知道，沒有你的手機也進不來。

> 📍 **在哪裡**：🖱 左側「⚙️ 設定」→ 第二個區塊 **「🔐 雙重驗證（MFA）」**

#### 🖱 步驟：開啟

| 步驟 | 操作 |
|---|---|
| 1 | 先在手機商店安裝一個 Authenticator App（Google Authenticator、Microsoft Authenticator、Authy 都可以）|
| 2 | 電腦上點左側「⚙️ 設定」，往下找到「🔐 雙重驗證（MFA）」|
| 3 | 點紫色 **「啟用設定」** 按鈕 |
| 4 | 畫面出現一串**密鑰**（英數字）和一個「開啟 otpauth 連結」的連結 |
| 5 | **把密鑰抄在紙上收好**（非常重要，理由見下方「手機掉了怎麼辦」）|
| 6 | 在手機 App 選「手動輸入密鑰」，把那串字打進去（帳號名稱隨便打，例如 Ouvoca）|
| 7 | 手機上會出現 6 位數字，把它填進網頁上的「6 位驗證碼」欄 |
| 8 | 點綠色 **「啟用」** |

成功會看到綠字 **「MFA 已啟用」**，區塊變成一個開關，顯示「MFA 已啟用（關閉需輸入驗證碼）」。

#### 👀 下次登入會多一步

```
┌─────────────────────────────────────────┐
│  🔐 alin 已啟用雙重驗證 —                │
│     請輸入 Authenticator App 的 6 位驗證碼│
│                                          │
│  驗證碼                                   │
│  ┌──────────────────────────────────┐   │
│  │        0 0 0 0 0 0               │   │
│  └──────────────────────────────────┘   │
│                                          │
│         [   驗證並登入   ]               │
│    ← 返回重新輸入帳號密碼                 │
└─────────────────────────────────────────┘
```

> ⏱ 打完密碼後，**要在 5 分鐘內**輸入驗證碼；超過會失效，出現「MFA 挑戰已失效，請重新登入」，回去重打帳密即可。

#### 🖱 步驟：關閉

在「⚙️ 設定 → 🔐 雙重驗證」的欄位輸入**目前的 6 位驗證碼**，再把開關關掉。**沒有驗證碼就關不掉**（防止別人趁你離開位置時關掉）。

#### 📱 手機掉了怎麼辦？（一定要先看）

**Ouvoca 目前沒有備用碼機制。** 手機不見 = 沒有驗證碼 = 自己既登不進去也關不掉 MFA。
所以請**先做**下面任一種預防：

| 預防做法 | 說明 |
|---|---|
| ✅ **抄下密鑰收好** | 啟用時畫面上那串英數字。換手機時在新手機的 App「手動輸入密鑰」就能復原 |
| ✅ **兩支手機都掃同一組密鑰** | 例如自己的手機 + 辦公室備用機，兩支產生的驗證碼會一樣 |

已經掉了才發現沒抄？**只能找 IT**：請 IT 到資料庫把該帳號的 `mfa_enabled` 設為 false、`mfa_secret` 清空
（`users` 資料表）。**系統沒有提供「管理員幫別人重設 MFA」的按鈕或指令**，所以會需要一點時間，請提早準備。

#### ❓ 常見問題

| 你看到的訊息 | 意思 | 你該做什麼 |
|---|---|---|
| 「驗證碼錯誤」 | 打錯，或手機時間不準 | 重新看一次手機上的數字（每 30 秒換）；到手機「設定 → 日期與時間」開啟「自動設定」|
| 「嘗試次數過多，帳號已暫時鎖定，請 15 分鐘後再試」 | 驗證碼連續錯太多次也會**觸發帳號鎖定** | 等 15 分鐘，或請 IT 解鎖（次數上限見 §3.34 的 `security.login_lockout_threshold`）|
| 「請先呼叫 /mfa/setup 取得 secret」 | 還沒點過「啟用設定」就想啟用 | 回去點「啟用設定」 |
| 「MFA 挑戰已失效，請重新登入」 | 超過 5 分鐘沒輸入驗證碼 | 重新輸入帳號密碼 |

---

### 3.33 💾 備份與還原 ✨ v3.62 新增

**這是什麼？** 把整個資料庫複製成一份快照存起來。電腦壞掉、有人誤刪一整批資料時，可以倒回去。

> 📍 **在哪裡**：🖱 左側「⚙️ 設定」→ 第三個區塊 **「💾 備份管理」**

#### 🖱 步驟：立即備份

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「⚙️ 設定」 |
| 2 | 找到「💾 備份管理」區塊 |
| 3 | 點右上角藍色 **「立即備份」** |
| 4 | 等它跑完（按鈕會變成「備份中…」）|

成功會看到綠字 **「✅ 備份完成：backup-20260804-031500-ouvoca.db」**，下方表格多一列（檔名 + 大小）。

> 🕒 **平常不用手動按**：系統預設 **每天凌晨 03:00 自動備份，保留 30 天**。
> 時間和天數都能改，見 §3.34（`backup.schedule` / `backup.retention_days` / `backup.enabled`）。
> 建議「做大動作之前」（大量匯入、清資料、系統升級）自己先按一次「立即備份」。

#### 🖱 步驟：還原（⚠️ 危險動作）

> 🚨 **還原會把現在的資料整個蓋掉，而且不可逆。**
> 還原後，備份時間點**之後**輸入的所有單據、庫存異動、傳票**全部消失**。
> 除非你確定要放棄這段期間的資料，否則不要按。做之前請先「立即備份」一次現況。

| 步驟 | 操作 |
|---|---|
| 1 | 在備份列表找到要還原的那一列（檔名裡就是日期時間：`backup-20260804-031500-…`）|
| 2 | 點紅色 **「還原」** |
| 3 | 跳出確認：**「⚠️ 從 … 還原會覆蓋目前資料庫且不可逆！確定？」** |
| 4 | 真的確定才按「確定」 |
| 5 | 看到「✅ 已還原 …（救援檔 pre-restore-…，請重新整理）」後，**按 F5 重新整理**，並請 IT 重啟服務 |

> 💡 **系統有幫你留一條後路**：還原之前，它會先把「現在的資料庫」另存成一個 `pre-restore-…` 的救援檔。
> 萬一還原錯了，請 IT 從那個檔案救回來。

#### 🖱 步驟：刪除備份

點該列灰色 **「刪除」**，跳出「刪除備份 …？」按確定。舊備份超過保留天數本來就會自動刪，平常不用手動清。

#### 💬 用講的版本

| 你說的話 | 系統做的事 |
|---|---|
| `有哪些備份` / `上次備份什麼時候` | 列出全部備份（時間 / 大小 / 是否可還原）|
| `現在備份一下資料庫` | 建立備份（出確認卡）|
| `還原到 backup-20260804-031500-ouvoca.db` | 還原（出確認卡，卡上會再警告一次不可逆）|

#### ❓ 常見問題

| 你看到的訊息 | 意思 | 你該做什麼 |
|---|---|---|
| 「PostgreSQL 環境不支援檔案快照；請使用資料庫原生備份（pg_dump）。」 | 你們公司用的是 PostgreSQL，不是內建的檔案型資料庫 | 這頁的備份功能用不到，請 IT 用資料庫自己的備份工具 |
| 「備份檔不是有效的 SQLite 資料庫，拒絕還原」 | 那個檔案壞了 | 換一個較早的備份 |
| 「⚠️ 更新失敗（可能權限不足）」 | 你的角色沒有備份權限 | 找主管開權限（`system.backup.create` / `system.backup.restore`）|
| 尚無備份 | 從來沒備份過，或自動備份被關掉 | 先按一次「立即備份」，並檢查 §3.34 的 `backup.enabled` |

---

### 3.34 ⚙️ 系統組態 14 項 ✨ v3.60 新增

**這是什麼？** 公司名稱、稅率、備份時間、密碼打錯幾次鎖帳號……這些「整套系統共用的設定」現在集中在一個地方，
**不用改程式、不用重開機**。

> 📍 **在哪裡**：🖱 左側「⚙️ 設定」→ 第四個區塊 **「⚙️ 系統組態」**（畫面上顯示 6 項）<br>🔧 其餘 8 項要找 IT（公司抬頭與時區另有對話指令，見下方黃框）

#### 📖 14 項設定一覽（附白話解釋）

| 設定代號 | 白話意思 | 預設值 | 設定頁改得到？ |
|---|---|---|---|
| `company.name` | 公司名稱（系統層預設值）| 我的公司 | ✗ |
| `company.tax_id` | 公司統一編號（系統層預設值）| （空白）| ✗ |
| `company.address` | 公司地址（系統層預設值）| （空白）| ✗ |
| `company.phone` | 公司電話（系統層預設值）| （空白）| ✗ |
| `currency.default` | 預設幣別 | TWD | ✗ |
| `tax.vat_rate` | 營業稅率（`0.05` 就是 5%）| 0.05 | ✅ |
| `finance.credit_limit_check` | 開銷售單時**要不要檢查客戶信用額度** | 開 | ✅（開關）|
| `timezone.default` | 時區（系統層預設值）| Asia/Taipei | ✗ |
| `locale.default` | 預設語言 | zh-TW | ✗ |
| `backup.enabled` | **自動備份開關** | 開 | ✅（開關）|
| `backup.schedule` | 每天幾點自動備份 | 03:00 | ✅ |
| `backup.retention_days` | 備份保留幾天 | 30 | ✅ |
| `security.login_lockout_threshold` | **密碼打錯幾次就鎖帳號**（鎖 15 分鐘）| 5 | ✅ |
| `ai.daily_limit_per_user` | 每人每天最多問 AI 幾次 | 200 | ✗ |

> ⚠️ **印在 PDF 上的公司抬頭不是在這裡改的！**
> 單據 PDF（報價單 / 採購單 / 銷售單 / 出貨單）上的公司名稱、統編、地址、電話，
> 以及 AI 回答日期時間用的時區，走的是另一份「**公司資料**」。要改請在「💬 AI 助手」講：
>
> - `公司叫長江精密股份有限公司，統編 12345678，地址台北市信義區信義路五段 7 號，電話 02-1234-5678`
> - `公司時區設 Asia/Taipei`
>
> 兩句都會跳確認卡，按確認後**之後**產生的 PDF 才會換成新抬頭（已經印好的不會回頭改）。
> 上面表格裡那四個 `company.*` 和 `timezone.default` 是系統層的預設值，改了它們**不會**影響 PDF 抬頭。

#### 🖱 步驟：改設定頁上的 6 項

| 步驟 | 操作 |
|---|---|
| 1 | 點左側「⚙️ 設定」，往下找到「⚙️ 系統組態」 |
| 2 | **開關類**（信用額度檢查、自動備份）：直接點右邊的開關 |
| 3 | **數值類**（稅率、備份保留天數、登入鎖定次數、備份時間）：點輸入框改數字，**改完點空白處（游標離開欄位）就會自動存** |
| 4 | 存好會出現綠字 **「✅ tax.vat_rate 已更新」** |

#### 👀 你會看到什麼

每一列下方有一行小灰字，例如 `security.login_lockout_threshold`，開關類還會標「**（來源 db）**」：

| 來源 | 意思 |
|---|---|
| `default` | 還沒人改過，用系統預設值 |
| `db` | 有人在這頁改過，存在資料庫 |
| `env` | **IT 在伺服器的環境變數寫死了** —— 這種你在畫面上改也沒用，會被蓋掉，要找 IT |

> 優先序（畫面上那行小字寫的就是這個）：**環境變數 > 資料庫 > 預設值**。

#### 🔧 其他 8 項怎麼改

沒顯示在設定頁的 8 項（公司名稱、統編、地址、電話、幣別、時區、語系、AI 每日上限），
目前**畫面上沒有欄位，也沒有對應的 AI 指令**，要請 IT 用系統介面
`PUT /api/system/settings/{key}`（權限 `system.config.update`）。

> 💡 只有「公司抬頭」和「時區」有現成的對話指令可用（見上方黃框），因為那兩項是實際會被單據和 AI 用到的設定。

#### ❓ 常見問題

| 症狀 | 意思 | 你該做什麼 |
|---|---|---|
| 「⚠️ 更新失敗（可能權限不足）」 | 你的角色不能改系統組態 | 找主管開 `system.config.update` 權限 |
| 改了但沒有變 | 那一項來源是 `env`（被環境變數蓋掉）| 找 IT 改伺服器上的環境變數 |
| 稅率打 `5` 結果變成 500% | 稅率是**小數**：5% 要打 `0.05` | 改回 `0.05` |
| 改了 `company.name` 但 PDF 抬頭沒變 | 這是兩個不同的地方 | 用 AI 講「公司叫 ⋯，統編 ⋯」重設公司資料（見上方黃框），再重新產生 PDF |
| 員工一直被鎖帳號 | 鎖定次數設太低 | 把 `security.login_lockout_threshold` 調高（例如 10）；鎖定時間固定 15 分鐘 |

---

## 4. 對 AI 講話：4 種 CRUD 操作

**這是整本手冊最重要的章節，請仔細看完。**

ERP 系統能做的事，總結就 4 類：「**查 / 增 / 改 / 刪**」。
我們把這 4 類都做成「用打字就能完成」。下面每一類給你 1-2 個實例。

### 4.1 「查」（Read）— 不會跳確認卡，直接給答案

**最簡單，AI 直接告訴你答案，不會問你要不要確認**（因為沒動到資料）。

#### 實例 1：列出庫存低於安全庫存的零件

你輸入：

```
列出庫存低於安全庫存的零件
```

AI 回答（大約 3-5 秒）：

```
找到 3 個零件低於安全庫存：

┌────────────────┬────────┬──────────┬────────┐
│ 料號            │ 現有  │ 安全庫存  │ 缺口   │
├────────────────┼────────┼──────────┼────────┤
│ M6-BOLT-20     │  300  │   500    │  -200  │
│ SUS304-PLATE   │   12  │    50    │   -38  │
│ GEAR-A-3MM     │    0  │    20    │   -20  │
└────────────────┴────────┴──────────┴────────┘

建議優先補貨 GEAR-A-3MM（已斷料）。
```

#### 實例 2：問銷售狀況

你輸入：

```
本月銷售總額多少？前 3 大客戶是誰？
```

AI 回答：

```
本月（2026-05）銷售總額：NT$ 4,820,000

前 3 大客戶：
1. 中鋼結構    NT$ 1,800,000（37%）
2. 台達電子    NT$   980,000（20%）
3. 鴻海精密    NT$   720,000（15%）
```

> 💡 「查」的操作**不需要按確認卡**，AI 看到資料直接回給你。

---

### 4.2 「增」（Create）— 會跳 ConfirmCard 要你按確認

**會動到資料 → AI 一定會跳「確認卡」要你按。**

#### 實例 3：建立採購單

你輸入：

```
跟長江廠下 100 個 M6 螺絲，交期下週五
```

AI 回答（不會直接下單，而是跳一張卡）：

```
┌────────────────────────────────────────────────────┐
│  📋 ConfirmCard：請確認以下採購單                   │
├────────────────────────────────────────────────────┤
│  供應商    │ 長江精密工業（S-0023）                  │
│  料號      │ M6-BOLT-20（已從「M6 螺絲」對應到）     │
│  數量      │ 100                                     │
│  單價      │ NT$ 0.5（採用上次成交價）                │
│  金額      │ NT$ 50                                  │
│  交期      │ 2026-05-22（下週五）                    │
│  付款條件  │ 月結 30 天（沿用供應商預設）             │
├────────────────────────────────────────────────────┤
│         [ ✓ 確認下單 ]    [ ✏ 修改 ]    [ ✗ 取消 ]  │
│              90 秒後自動取消 ⏱ 88                    │
└────────────────────────────────────────────────────┘
```

**你的操作**：

| 你想 | 滑鼠點 |
|---|---|
| 沒問題，下單 | **[ ✓ 確認下單 ]** |
| 數量錯了想改 | **[ ✏ 修改 ]**（會回到對話讓你說「改成 200 個」） |
| 不要下了 | **[ ✗ 取消 ]** |

按「✓ 確認下單」後：

```
✅ 已建立採購單 PO-2026-0142
   發送通知給長江精密工業
   預計到貨：2026-05-22

   💡 如果搞錯了，90 秒內輸入「撤銷上一個動作」可以收回。
```

---

### 4.3 「改」（Update）— 也會跳 ConfirmCard

**改資料和新增一樣危險，AI 也會跳確認卡。**

#### 實例 4：修改銷售單交期

你輸入：

```
把 SO-2025-0042 的交期改到 6/10
```

AI 回答：

```
┌────────────────────────────────────────────────────┐
│  📋 ConfirmCard：請確認以下修改                     │
├────────────────────────────────────────────────────┤
│  銷售單   │ SO-2025-0042                            │
│  客戶     │ 中鋼結構（C-0007）                       │
│  品項     │ GEAR-A-3MM × 500                        │
│                                                     │
│  欄位     │ 交期                                    │
│  原值     │ 2026-06-03                              │
│  新值     │ 2026-06-10 ⚠ 延後 7 天                  │
├────────────────────────────────────────────────────┤
│         [ ✓ 確認修改 ]    [ ✏ 微調 ]    [ ✗ 取消 ]  │
└────────────────────────────────────────────────────┘
```

點「✓ 確認修改」後：

```
✅ SO-2025-0042 交期已更新為 2026-06-10
   已自動通知客戶聯絡人（透過 Email）
   90 秒內可撤銷
```

---

### 4.4 「刪 / 取消」（Delete）— ConfirmCard 紅色警示

**刪除是最危險的，AI 會用紅色卡片＋強警告。**

#### 實例 5：取消採購單

你輸入：

```
取消 PO-2026-0107
```

AI 回答：

```
┌────────────────────────────────────────────────────┐
│  ⚠️  ConfirmCard：取消採購單（不可恢復！）          │
├────────────────────────────────────────────────────┤
│  採購單   │ PO-2026-0107                            │
│  供應商   │ 大華精密工業                            │
│  金額     │ NT$ 28,500                              │
│  狀態     │ 已送出，尚未收貨                        │
│                                                     │
│  📌 影響：                                          │
│    - 採購單會標記為「已取消」                       │
│    - 系統會通知供應商（Email）                      │
│    - 庫存預定到貨數會 -300                          │
├────────────────────────────────────────────────────┤
│   [ ✓ 確定取消 ]    [ ✗ 不要，保留採購單 ]          │
└────────────────────────────────────────────────────┘
```

> 💡 注意：**檢驗單（QC）不能用 AI 刪除**——這是稽核紀錄，永遠保留。

---

## 5. ConfirmCard 確認卡

### 5.1 為什麼一定要有 ConfirmCard？

**因為 AI 會「幻覺」**（業界術語叫 Hallucination）。

什麼意思？AI 偶爾會「自己想像」資料。例如：
- 你說「跟長江廠下單」，AI 可能搞混成「跟長榮廠下單」
- 你說「100 個」，AI 可能聽成「1000 個」
- 你說「M6 螺絲」，但庫存裡有 M6-BOLT、M6-NUT、M6-WASHER 三種

如果沒有 ConfirmCard，AI 直接下單 → **你公司可能多花 10 倍的錢，或下錯廠**。

**ConfirmCard 的設計哲學**：
> AI 負責理解你的話、產生草稿；
> **人類負責最後一眼確認**，按下「確認」才真的執行。

### 5.2 ConfirmCard 上有什麼？

每張 ConfirmCard 都有：

| 區塊 | 內容 |
|---|---|
| **標題** | 要做什麼動作（建立 PO / 修改 SO / 取消 WO ...） |
| **摘要表格** | 所有欄位的對照（從哪解析出來的、值是多少） |
| **影響說明** | 這個動作會引起什麼連鎖反應 |
| **3 個按鈕** | ✓ 確認 / ✏ 修改 / ✗ 取消 |
| **倒數計時** | 90 秒不點 → 自動取消，避免你忘了 |

### 5.3 ConfirmCard 的 4 種風險等級

系統內部把工具分 4 級，不同風險顯示不同顏色：

| 等級 | 顏色 | 範例 | ConfirmCard 行為 |
|---|---|---|---|
| 🟢 **查（Read）** | 無卡片 | 「列出庫存」 | 直接給答案，不跳卡 |
| 🟡 **軟寫（Soft-write）** | 黃色 | 「儲存搜尋條件」 | 簡單卡片 + 簡單 Undo |
| 🟠 **硬寫（Hard-write）** | 橘色 | 「建立 PO」「改交期」 | **完整 ConfirmCard + 90 秒 Undo** |
| 🔴 **高危（Critical）** | 紅色 | 「刪客戶」「取消大額 PO」 | **紅色卡片 + 要打字確認** |

---

## 6. Slot-filling 反問機制

### 6.1 什麼是 Slot-filling？

英文「Slot」就是「欄位」。如果你話講得不完整，AI **不會自己編**，會反問你。

### 6.2 實例：欄位不完整時的對話

你輸入：

```
給中鋼下 PO
```

AI 回答：

```
好的，要建立採購單給中鋼。請問：

1. 要買什麼料？（料號或品名）
2. 數量多少？
3. 交期什麼時候？

也可以一次回答，例如：「M6-BOLT-20，1000 個，下週五」
```

你回：

```
M6 螺絲 1000 個下週五
```

AI 回答（部分還沒解析清楚）：

```
「M6 螺絲」對應到 3 個料號，請選一個：

   ┌─────────────────────────────────────┐
   │ ⓪ M6-BOLT-20（六角螺絲，最常用）     │
   │ ① M6-NUT（M6 螺帽）                  │
   │ ② M6-WASHER（M6 墊片）              │
   └─────────────────────────────────────┘

請輸入編號（例如「0」）或料號全名。
```

你回 `0` → AI 跳出完整 ConfirmCard。

### 6.3 為什麼要這樣設計？

**不會反問 = AI 亂猜 = 你公司下錯料**。
我們寧可多問你幾句，也不要讓你公司賠錢。

> 💡 **反問上限 3 次**：如果 AI 問 3 次還對不上你的意思，
> 它會說「請改用左側『採購』頁面的表單填寫」，把你帶到傳統介面。

---

## 7. 90 秒 Undo 撤銷

### 7.1 怎麼撤銷？

任何「增/改/刪」操作後，**90 秒內**你可以撤銷。

#### 方法 1：用講的

在 AI 對話框輸入：

```
撤銷上一個動作
```

或

```
取消剛才那筆
```

AI 回答：

```
✅ 已撤銷 PO-2026-0142 的建立
   採購單已標記為「已撤回」
   長江精密工業已收到撤回通知
```

#### 方法 2：點按鈕

執行完操作後，AI 回應下方會出現一個小按鈕：

```
✅ 已建立採購單 PO-2026-0142

   [ ↩ 撤銷（剩 67 秒）]
```

直接點它就好。

### 7.2 撤銷的限制

| 情況 | 能撤嗎？ |
|---|---|
| 90 秒內，採購單還沒收貨 | ✅ 可以 |
| 90 秒內，採購單已部分收貨 | ❌ 不行（已產生實體流程） |
| 超過 90 秒 | ❌ 請走正常「取消單據」流程 |
| 別人按確認的單，你想撤 | ❌ 只能撤自己按的 |

---

## 8. 4 個角色實戰

### 8.1 👔 王董（老闆）— 桌機看公司狀況

**情境**：早上 8:30 進辦公室，想知道昨天到今天公司怎樣。

**做法**：

| 步驟 | 操作 |
|---|---|
| 1 | 打開 Chrome，輸入網址 `http://localhost:5173` |
| 2 | 輸入帳號 `boss_wang` / 密碼，點「登入」 |
| 3 | 進入後預設停在 📊 **儀表板**，AI 智能摘要會自動跑出來 |

AI 智能摘要長這樣：

```
┌────────────────────────────────────────────────────┐
│ 📈 王董早安！今日重點摘要（2026-05-17 08:35）       │
├────────────────────────────────────────────────────┤
│                                                     │
│ ✅ 昨日出貨：12 單，營收 NT$ 580,000                 │
│ ⚠️  今日待釋放工單：3 張（最急 SO-2026-0042）        │
│ 🔴 庫存警示：M6 螺絲剩 300 個（< 安全 500）          │
│ 💰 應收帳款 DSO：48 天（較上月 +3）                  │
│                                                     │
│ [ 看詳情 ] [ 處理急單 ] [ 通知阿玲補貨 ]            │
└────────────────────────────────────────────────────┘
```

**接著想知道更多**，點左下「💬 AI 助手」輸入：

```
本月毛利率多少？跟上月比？
```

AI 回答：

```
本月毛利率：23.4%（NT$ 1,128K / NT$ 4,820K）
上月毛利率：21.8%
變化：+1.6 個百分點 📈

主要貢獻：CNC 加工件毛利率從 18% 提升到 22%
```

---

### 8.2 👨‍💼 小陳（業務）— 客戶面前 3 秒答題

**情境**：小陳在客戶辦公室，客戶問：「我上次跟你買的 GEAR-A 還有貨嗎？最快什麼時候到？」

**做法**：

| 步驟 | 操作 |
|---|---|
| 1 | 打開筆電 Chrome（已連好公司 VPN） |
| 2 | 登入後直接點左下「💬 AI 助手」 |
| 3 | 鍵盤打字 |

輸入：

```
中鋼結構的 GEAR-A-3MM 現有多少？最快可承諾交期？
```

AI 回答（3-5 秒）：

```
中鋼結構（C-0007）× GEAR-A-3MM：

   現有庫存：    420 件
   生產中：      150 件（WO-2026-0089，預計 5/22 完工）
   已預訂：       80 件
   ─────────────────────────
   可承諾數量：  490 件
   最快交期：    今天下單，5/23 出貨

   📌 上次給中鋼的單價：NT$ 4,500/件（2026-04-20）
```

**接著客戶說「要訂 300 個」**：

```
跟中鋼結構下 SO，GEAR-A-3MM × 300，單價沿用上次，交期 5/30
```

AI 跳 ConfirmCard，小陳點「✓ 確認」→ 30 秒內銷售單成立。

> 💡 小陳**看不到別的業務的客戶**，這是 RBAC（角色權限）的保護。

---

### 8.3 👨‍🏭 林廠長（生產主管）— 桌機 + 廠房大屏

**情境之一：辦公室桌機看狀況**

林廠長在辦公室打開儀表板，左側點「🏭 生產」，看到 War-Room Dashboard：

```
┌──────────────────────────────────────────────────────┐
│   🏭 生產 War-Room（即時更新）        2026-05-17 09:42│
├──────────────────────────────────────────────────────┤
│                                                       │
│   進行中工單：15 張    平均進度：68%                  │
│                                                       │
│   ⚠️  延遲警示：                                      │
│   ┌────────────────────────────────────────────┐    │
│   │ WO-2026-0073  軸承座 B    落後 2 天 🔴      │    │
│   │ WO-2026-0081  外殼蓋     落後 1 天 🟡       │    │
│   └────────────────────────────────────────────┘    │
│                                                       │
│   📦 缺料風險：                                       │
│   ┌────────────────────────────────────────────┐    │
│   │ M6-BOLT-20  剩 300，本週需求 800 → 缺 500   │    │
│   └────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────┘
```

**情境之二：用講的釋放工單**

林廠長對 AI 說：

```
釋放 WO-2026-0091，指派給 CNC-A 機台
```

AI 跳 ConfirmCard 確認，點「✓」→ 工單下到現場。

---

### 8.4 👩‍💻 阿玲（採購兼倉管）— 桌機 + USB 條碼槍

**情境之一：用講的下採購單**

阿玲收到林廠長 Email「補 M6 螺絲」，她對 AI 說：

```
跟長江精密下 M6-BOLT-20 × 2000，交期下週三，月結 30
```

AI 跳 ConfirmCard，阿玲確認後 PO 立刻成立。

**情境之二：收料時用 USB 條碼槍**

| 步驟 | 操作 |
|---|---|
| 1 | 打開「🛒 採購」頁面 → 找到 `PO-2026-0142` |
| 2 | 點「收料」按鈕，畫面跳出收料表單 |
| 3 | 把 USB 條碼槍接到電腦 USB |
| 4 | 對著供應商送來的箱子條碼**「嗶」一聲** |
| 5 | 系統自動帶出料號和預計數量 |
| 6 | 鍵盤輸入「實收數量」 |
| 7 | 點「確認入庫」 → 庫存 +N，PO 標記「已收貨」 |

> 💡 USB 條碼槍進階用法（如多倉位、批號管理）排在 **Phase 2**，本期先做基本「嗶一聲帶料號」。

---

## 9. 三軌授權

Ouvoca 採用三軌授權，**選對的省幾百萬**。

| 軌道 | 適合誰 | 費用 | 重點 |
|---|---|---|---|
| 🟢 **AGPL** | 想看原始碼、不介意分享自己修改 | 免費 | 完全開源 |
| 🌱 **Small Business** | 50-100 人廠、≤ 20 個同時上線的人 | **完全免費** | **含閉源 connector**（鼎新/正航接口）<sup>※</sup> |
| 🔵 **Commercial** | > 20 人同時上線、ISV、SaaS 業者 | 議價 | 解除 AGPL 條款限制 |

> ⚠️ <sup>※</sup> **建議閱讀**：connector 提供的是**技術連線元件**。若您要把 Ouvoca 接到**現有商用 ERP**（例如 Workflow / ChengHang / SAP B1 / Vitals 等廠商之產品），各廠商之授權合約對「以共用 / 服務帳號連線」之規定可能不同；具體請依您與該廠商之合約為準。建議於啟用前先與原 ERP 廠商書面確認授權範圍，必要時購買相應之整合授權。Ouvoca **不參與、不代理**與原 ERP 廠商之合約 / 授權事務，於適用法律所允許之最大範圍內，Ouvoca 對客戶未取得適當授權即啟用連線所衍生之後果不承擔責任。詳見 [`docs/EXTERNAL_DB_LICENSING_NOTICE_ZH.md`](./EXTERNAL_DB_LICENSING_NOTICE_ZH.md)。

### 怎麼申請 Small Business 軌？

完全免費，但要登記。請看：

- 📄 **`LICENSE-COMMERCIAL.md`**（在程式根目錄）
- 📄 **`docs/COMMERCIAL_LICENSING_FAQ_ZH.md`**

寄信給授權窗口，附上「公司名 + 員工數 + 預期同時上線人數」就會收到 Small Business 條款的書面授權。

---

## 10. 常見問題

### Q1：我打字打錯字 AI 會看得懂嗎？

→ 大多看得懂。「M6 螺絲」「m6 buoy 」「m6 螺絲」AI 都會嘗試對應。
看不懂時 AI 會反問你。

### Q2：AI 回答錯了怎麼辦？

→ 如果是「查」回答錯：再問一次，把問題說清楚。
如果是「增/改/刪」的 ConfirmCard 內容不對：點「✏ 修改」或「✗ 取消」，不要按確認。

### Q3：對話歷史會保留多久？

→ 預設 30 天（IT 可調）。
要看歷史：右上角 👤 頭像 → 「我的對話紀錄」。

### Q4：可以切換角色嗎？例如業務想看廠長視角？

→ 一個帳號對應一個角色，**不能自己切**。
要看別人的視角請請 IT/老闆在「⚙️ 設定 → 使用者權限」加你的角色。

### Q5：我可以匯出 Excel 嗎？

→ 列表頁右上角有「📥 匯出 CSV」按鈕（CSV 可用 Excel 打開）。
Excel `.xlsx` 直出排在 Phase 2。

### Q6：忘記密碼怎麼辦？

→ 找 IT 重設。自助密碼重設（Email 連結）排在 Phase 2。

### Q7：可以用語音輸入嗎？

→ **Phase 4 才會做**（Whisper 語音轉文字）。
目前只能打字。

### Q8：可以多廠共用一套系統嗎？

→ 可以。叫做「MESH 多廠」，每個廠資料獨立但 HQ 可以聚合查詢。
詳細請看 `NETWORK_DEPLOYMENT_ZH.md`。

### Q9：AI 不回應怎麼辦？

→ 看 §11 疑難排解 → 11.3。

### Q10：商業使用前要做什麼？

→ 看第 9 章「三軌授權」+ `LICENSE-COMMERCIAL.md`。

---

## 11. 疑難排解

### 11.1 進不去登入頁

| 症狀 | 怎麼解 |
|---|---|
| 瀏覽器顯示「無法連線到此網站」 | 確認 IT 已啟動後端（`docker compose ps`） |
| 503 / 504 錯誤 | 請 IT 重啟：`docker compose restart backend` |
| 401 一直跳出來 | 按 F12 → Application → Storage → Clear site data，重新整理 |
| **Windows：頁面打得開，但表單 / 報表 / 列印都失敗** | **改用 `http://127.0.0.1:5173`**（不用 `localhost`）— Windows Docker Desktop 偶有 `localhost` 解析到 IPv6 ::1 + CORS 擋掉的問題；v3.25.7 已修預設綁 127.0.0.1 + CORS 補 127.0.0.1 變體 |
| F12 console 一直跳 CORS 錯誤 | 同上，改用 `http://127.0.0.1:5173`；若仍不行，請 IT 在 `backend/.env` 加 `CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173","http://你的網域:5173"]` |

### 11.2 看不到資料

| 症狀 | 怎麼解 |
|---|---|
| 列表全空 | IT 還沒載入示範資料（`docker compose exec backend python -m scripts.seed`） |
| 只看到自己建立的客戶 | **這是設計**，不是 bug。RBAC 保護你的客戶不被別人看見 |
| 點按鈕跳 403 forbidden | 你的角色沒這個權限，請 IT 加角色 |

### 11.3 AI 助手不回應

| 症狀 | 怎麼解 |
|---|---|
| 跳出「🤖 AI 助手還沒啟用」3 步驟卡 | **不用找 IT**！自己進 ⚙️ 設定 → AI 設定區，按引導申請貼 key（看 §3.5 + [`HOW_TO_GET_LLM_API_KEY_ZH.md`](./HOW_TO_GET_LLM_API_KEY_ZH.md)）|
| 右上角顯示「⚠️ AI 未設定」 | 同上，點 ⚠️ 自動跳到設定頁 |
| 顯示「API key 無效」/「quota 用完」 | 進 ⚠️ 設定頁更新 key（不需 IT 改 .env）|
| 等很久（超過 30 秒） | LLM 在思考中，最多等 60 秒；超過請 IT 看 log |
| AI 一直問同一個問題 | 反問 3 次仍對不上 → AI 會建議你改用左側表單頁面 |
| **不知道這頁怎麼用** | 點**右下角 💡 浮球**問 AI「這頁怎麼用？」（§3.7）|

### 11.4 ConfirmCard 異常

| 症狀 | 怎麼解 |
|---|---|
| 倒數結束自動取消了 | 重新對 AI 講一次就好 |
| 點「✏ 修改」沒反應 | 重新整理頁面（F5），再對 AI 講一次 |
| 點「✓ 確認」後一直轉圈 | 看右下角是否有錯誤訊息；找 IT 看後端 log |

### 11.5 撤銷失敗

| 症狀 | 怎麼解 |
|---|---|
| 「超過 90 秒，不能撤銷」 | 請走正式「取消單據」流程 |
| 「該動作已被下游流程使用」 | 例如 PO 已部分收料，要先做退料才能撤 |
| 「您不是執行者，不能撤銷」 | 請當初按確認的人來撤 |

### 11.6 還是不行？

聯絡內部 IT，並提供：
- 截圖（按 Win+Shift+S 框選）
- 你輸入的指令
- 錯誤訊息（如有）
- 時間（幾點幾分）

或寫 Email 到貴公司 ERP 維運窗口。

---

**本手冊版本**：v3.70（2026-08-04）— §3.20–3.34 涵蓋 v3.60–v3.69 全部新功能（請購/收料/領料/退貨、RFQ 詢價、批號序號追溯、三大報表、401/405 報稅、3-Way Match、固定資產折舊、COGS 結轉、MFA、備份還原、系統組態）
**對應英文版**：[`USER_MANUAL_EN.md`](./USER_MANUAL_EN.md)
**配套文件**：[`CONVERSATIONAL_ERP_DESIGN_ZH.md`](./CONVERSATIONAL_ERP_DESIGN_ZH.md)（架構設計）／[`COMMERCIAL_LICENSING_FAQ_ZH.md`](./COMMERCIAL_LICENSING_FAQ_ZH.md)（授權問答）
